# The image installs the PUBLISHED @mailwoman/* packages from npm — it never copies monorepo source.
# Keep the build context tiny: ignore everything, then re-allow only the two files the Dockerfile COPYs.
*
!docker/package.json
!docker/server.mjs