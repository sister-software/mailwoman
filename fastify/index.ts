/**
 * @copyright Sister Software
 * @license AGPL-3.0
 * @author Teffen Ellis, et al.
 *
 *   `@mailwoman/fastify` — Fastify plugin for mailwoman.
 *
 *   v0.0.1 is a name-reservation placeholder published to configure npm Trusted Publishing.
 *   The first functional release registers geocode/parse/POI routes and a
 *   `fastify.mailwoman` decorator over the local runtime pipeline.
 */

export const MAILWOMAN_FASTIFY_STATUS = "placeholder" as const
