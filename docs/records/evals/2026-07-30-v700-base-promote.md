# 2026-07-30 — model 7.0.0 promote (v4.0.1-base-60k, the from-scratch base)

**Verdict: PROMOTED** (npm 8.3.0). The v8.3.0 arc's golden master: from scratch, 60k cosine s42,
one recipe, one provenance — the first base since the v264 era, and the first mailwoman model
whose locality evidence channel actually trained (#1349).

## The two-run envelope, as executed

- **Run 1 (v4.0.0)**: 16/17 golden; fr.cedex 90.5 (the named watch retired); gauntlet regression
  failed 3 FR venue-led fixtures + si-sentinel. Root cause of the venue family: the feed NEVER
  admitted a venue shard — the shipped lineage's venue competence was inheritance from ancestral
  feeds, which from-scratch X-rayed away.
- **Run 2 (v4.0.1, the pre-registered single-delta contingency)**: the FR-order-aware
  synth-house-venue shard (116,244 real BAN+situs tuples) folded at 2.0. All three venue
  must-clears CLEARED; three tracked venue cases promoted to pass; every other class within noise
  of run 1 (several floors up); P0 grid flat 40k→60k in both registers.

## Ship configuration

- Model: `model-v401-base-step-060000-int8.onnx` (md5 `c968c24a…`), tokenizer v0.9.0-multisplice
  (unchanged), street-type lexicon v3 (unchanged), **locality-surface lexicon v6** — trained on
  v7, shipped with v6 per the pre-registered G8 contingency: the DE fold read net-negative on its
  own board (homonym −0.113 asis / −0.130 lower), so DE surfaces run the trained absence identity
  (v7 ⊂ v6+DE verified: zero removed/changed US-FR entries; the ship-config regrade IMPROVED
  several US floors — the DE painting was mildly contaminating).
- **Fisher artifact**: fisher-diag-v1 (2,000 batches, all 39,261,743 trainables; md5 `79ede288…`),
  HF/R2-distributed beside the weights — the EWC brake for every fine-tune off this base.

## Gate record

- Golden (ship config): 16/17 on v6.0.0-shipped-baseline + bare-street-intact + mask-regression;
  the one nominal miss is us.street_prefix 91.3 vs the 96.0 **fallback-margin artifact** (n=25;
  four spelled-out-directional recall misses, precision 100; the 9-probe directional-homograph
  set shows zero over-splitting cost). Clears the new spec.
- **New gate spec: `v7.0.0-base`** — cut from this candidate's ship-config readings with
  support-aware WILSON margins (the v6 spec's 2.0pp fallback and the boundary-reading
  under-margining both retired). Final verdict vs the new spec: PASS.
- Gauntlet (the pre-ship flip): regression **47/47 gated**, metamorphic PASS (5 tracked xfails,
  one fewer than shipped), held-out PASS.
- G8 (the DE fragment board, new): the v7-fold value gate MISSED → the revert executed. The
  board itself (overture-fragments-de) joins the standing instruments.
- Capability manifest regenerated on the shipped bytes (register-pinned formatted).

## Named watch (one)

- **si-sentinel-ucakar** — SI diacritic digit-split ("Ulica bratov Učakar 54"): the raw model
  split is byte-identical to shipped, which passes only via the word-consistency heal's vote.
  Designed fix = the char-path lineage (no subword fragmentation; the #825 thesis the JP probe
  validated at 0.9925). Retires when a char-lineage Latin model grades.

## Intake corrections recorded (the honest ledger)

- Run 1's gauntlet was initially reported as 3 fails; it was 4 (si-sentinel under-counted).
- The operator's 9-probe set was first "graded" against a stale regression DB — absence of
  failure was misread as passing. True intake: the 3 bare directional rows pass on both models;
  **all 6 venue-led rows fail on BOTH** (the GB tail "London EC3N 1DE" matches neither trained
  venue template) → filed #1366 as improvement targets; the natural first exercise of the B11
  fine-tune template (init_from 7.0.0 + the Fisher brake + these six as the target board).

## Follow-ups riding the release train

#1366 GB venue increment · tasks #25/#26 (SP vocab pruning probe; SP 0.2.2 WASM) · λ calibration
on the first post-base fine-tune · CJK Phase 3 (the JP full shard).
