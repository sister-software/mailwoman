# Phase 8 Thread E — learned span proposer (v0.5.1 candidate)

**Status:** scoped rather than implemented. v0.5.0 ships the rule-based phrase grouper (`@mailwoman/phrase-grouper`); this doc lays out the learned counterpart for v0.5.1 (or v0.5.0 stretch, time permitting).

**Branch / package convention:** `feat/v0.5.1-learned-span-proposer`; new workspace `@mailwoman/phrase-grouper-learned` (or extension of the existing workspace blocked by an env / opts flag).

**Depends on:** Thread B (synthetic adversarial corpus with span-boundary labels) + Thread A (tokenizer v0.5.0). Cannot start until both are landed.

## Why a learned version

The rule-based v1 covers the common shapes (NUMERIC, STREET_PHRASE, LOCALITY_PHRASE, REGION_ABBREVIATION, POSTCODE, VENUE_PHRASE, HYPHENATED_COMPOUND) at predictable, explainable cost. Its blind spots are:

- **Inputs without capitalization cues:** all-lowercase OCR output, voice-to-text, and partially typed autocomplete input. The rule pack relies heavily on capitalization as the LOCALITY_PHRASE / VENUE_PHRASE signal.
- **Non-Latin scripts:** CJK / Cyrillic / Arabic have no capitalization signal at all. Rule packs would need hand-crafted per-script alternatives, while a learned model trained on a multi-script corpus can generalize without them.
- **Compositional novelty:** `NY-NY Steakhouse` works because both `NY-NY` (hyphen-compound) and `Steakhouse` (venue marker) are explicit cues. `Lou's Backyard BBQ Pit Stop` works only if `BBQ` is in the venue-marker list, and that list is unbounded.

A learned proposer is the bitter-lesson-safe approach: train a small (1-2M parameter) span-prediction model on the corpus's segment-boundary labels and let it discover the joint cues the rule pack approximates manually.

## Architecture sketch

**Input:** the same `(NormalizedInput, QueryShape, LocaleHint)` the rule version takes.

**Output:** the same `PhraseProposal[]` interface (`{ span: Section; kindHypothesis: PhraseKind; confidence: number }`). The downstream Stage 5 reconciler does not need to know which kind of grouper produced the proposals.

**Model:** small encoder-only transformer.

- **Tokenizer:** the v0.5.0 multi-script sentencepiece tokenizer (Thread A). It is shared with the main classifier, without a separate vocab.
- **Embedding:** ~256-dim per token. Reused from the main classifier as a starting point (LoRA / linear adapter on top), or trained from scratch with a tied vocabulary.
- **Encoder:** 3-4 transformer blocks, hidden 256, 4 attention heads. Total ~1-2M params (a fraction of the main classifier's ~10M).
- **Heads:** two parallel heads:
  - **Boundary head:** per-token binary classifier "is this token a phrase boundary?" (BIO-shaped: begin, inside, outside).
  - **Kind head:** per-token 7-way softmax over the `PhraseKind` taxonomy. Predicted at the begin-token of each proposed phrase.
- **Decoding:** Viterbi over the boundary head emissions, then read off `kindHypothesis` at each phrase's begin token. Top-k proposals (configurable, default k=5) emitted with calibrated confidence.

## Training data

Training data comes from Thread B (corpus-v0.4.0) in two forms:

1. **Derived labels from the existing BIO corpus.** Each contiguous run of same-tag tokens is one phrase; map the BIO tag to a `PhraseKind` via a lookup table (e.g. `B-locality ... I-locality` → LOCALITY_PHRASE). These labels cost nothing at training time and need no new labeling. The mapping is lossy at the edges (POI tags collapse to VENUE_PHRASE), but the loss falls in the structural-shape vocabulary, which this model does not need to distinguish.
2. **Synthetic adversarial expansion.** The kryptonite-catalogue generation pipeline (Thread B) prompt-engineers DeepSeek to produce inputs where the rule-based grouper systematically gets boundaries wrong. The learned model trains on those cases as hard negatives.

Validation uses a held-out subset of the kryptonite catalogue plus the v0.4.0 bio_slip class (the 6% of cases where boundaries were demonstrably wrong in production).

## Calibration

The reconciler weighs phrase proposals against classifier proposals and resolver candidates. Confidence calibration matters more here than in the rule version, because the reconciler multiplies scores. Apply training-time temperature scaling against the held-out kryptonite subset, and report ECE on the model card alongside accuracy.

## When to ship

- **v0.5.0 stretch:** if Thread C (classifier retrain) finishes inside the GPU budget and the rule-based grouper needs help on a meaningful share of the eval (e.g. non-Latin queries show poor proposals). The v0.5.0 eval decides this, rather than a prior commitment.
- **v0.5.1 fallback:** if v0.5.0 ships on the rule version alone. Allocate ~3-5 days of compute + engineering for the learned version once the corpus is in place.

## Out of scope for this doc

- **Multi-locale ensembling:** one model per locale vs one model handling all locales. Before evaluation, lean toward one shared model trained on the full multi-script corpus, because the tokenizer and the encoder already do the locale-aware work.
- **Joint training with the main classifier:** phrase-proposer embeddings could pass into the classifier as conditioning. Thread C mentions this as the eventual integration. Whether the proposer trains jointly or stays a frozen feature extractor is a v0.6.0+ question.
- **Replacing the rule version.** The rule version stays in tree as the explainable fallback + the test oracle. The learned version is additive. Both can coexist behind a runtime flag, and the reconciler can consume proposals from either (or both, with source attribution).

## See also

- [`PHASE_8_v0_5_0_fresh_slate.md`](./PHASE_8_v0_5_0_fresh_slate.mdx) — the v0.5.0 master plan
- [`the-knowledge-ladder.md`](../../site-2026-08/understanding/our-approach/the-knowledge-ladder.mdx) — where Stage 2.7 sits in the layered decomposition
- [`STAGES.md`](https://github.com/sister-software/mailwoman/blob/main/docs/engineering/reference/STAGES.mdx) — the formal pipeline interface
