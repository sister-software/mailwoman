/test-results
# Dependencies
/node_modules

# Production
/build

# Runtime assets the explainers read (staged by the runtime-assets plugin or served from R2; not for git).
/static/mailwoman/*.onnx
/static/mailwoman/*.model
/static/mailwoman/*.db
/static/mailwoman/*.bin
/static/mailwoman/*.json
# sql.js-httpvfs worker/wasm — staged into the Pages deploy at build time by the runtime-assets plugin.
/static/mailwoman/sqljs/
# MapLibre's tile worker + the module it imports — staged same-origin by the runtime-assets plugin.
/static/mailwoman/maplibre/

# OpenAPI spec copies — build products of the `prebuild` script (four `openapi` CLI subcommands),
# never handwritten or checked in. See articles/api.mdx's "Drop-in server specifications" section.
/static/openapi/*.json

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
/playwright-report
