---
title: What ships today
description: Versions, packages, model artifacts, locale coverage, and the limits — as of Mailwoman 10.0.0.
role: reference
source-of-truth: package.json versions + `mailwoman doctor` + the eval ledger at evals/scores-by-version.json
---

# What ships today

:::info[Verified as of release 10.0.0, model 9.1.0]

Every figure on this page was re-measured on 2026-09-14 against the published artifacts. The release
process refuses to publish if the model version in this box is older than the model the release
ships.

:::

## Scope

This page records what the current release contains: the version, the packages a consumer installs,
the model artifacts and their measured sizes, the supported locales, and the limits. It does not record
per-tag accuracy. Those numbers are in the eval ledger
([`evals/scores-by-version.json`](https://github.com/sister-software/mailwoman/blob/main/evals/scores-by-version.json))
and in the dated eval reports next to it.

Every artifact size below was measured on 2026-09-14 against the files this release publishes.

## Version

Every published workspace, including the model bundles, releases together under one version number. A
weights package version therefore follows the release instead of the model's own version history.

| Fact                | Value                                                                 |
| ------------------- | --------------------------------------------------------------------- |
| Release             | `10.0.0`                                                              |
| Model version       | `9.1.0` — the Latin base, unchanged since release `9.1.0`             |
| Node floor          | `>=24.18.0` (the `engines` field on the `mailwoman` entry package)    |
| Module format       | ESM only                                                              |
| License             | `AGPL-3.0-only OR LicenseRef-Commercial`                              |
| What npm serves now | `npm view mailwoman version`. Check it rather than trusting this page |

## Packages

These are the packages a consumer installs directly. The remaining workspaces are pipeline stages
and build tooling, which are installed as dependencies of these packages.

| Package                                                             | What it is                                                                                                                     |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `mailwoman`                                                         | The CLI and `createRuntimePipeline`, the recommended entry point                                                               |
| `@mailwoman/neural`                                                 | The inference runtime: SentencePiece tokenizer, ONNX execution, decoder wiring                                                 |
| `@mailwoman/neural-weights-<locale>`                                | The Latin model bundles. Data only. Published: `en-us`, `fr-fr`, `en-gb`, `en-au`, `en-nz`, `de-de`, `es-es`, `it-it`, `en-in` |
| `@mailwoman/neural-weights-cjk`                                     | The character-path model for Japanese, Korean and Chinese, with the `ja-jp` and `zh-cn` data-only overlays beside it           |
| `@mailwoman/core`                                                   | Tokenization, decoder, calibration, the policy registry, and the bundled reference data                                        |
| `@mailwoman/resolver`                                               | Walks a parsed tree and decorates it with coordinates and attribution                                                          |
| `@mailwoman/resolver-wof-sqlite`                                    | The Node gazetteer backend, over `node:sqlite`                                                                                 |
| `@mailwoman/neural/web-loader`                                      | The browser inference runtime, over `onnxruntime-web`                                                                          |
| `@mailwoman/resolver-wof-wasm`                                      | The browser gazetteer backend, over `@sqlite.org/sqlite-wasm`                                                                  |
| `@mailwoman/api`                                                    | The native HTTP surface: `/v1/parse`, `/geocode`, `/batch`, `/resolve`, `/format`                                              |
| `@mailwoman/nominatim`, `@mailwoman/photon`, `@mailwoman/libpostal` | Drop-in servers that answer on the shapes those tools already use                                                              |
| `@mailwoman/mcp`                                                    | An MCP server exposing parse, geocode, and POI search to agents over stdio                                                     |

## Model artifacts

`@mailwoman/neural-weights-en-us` publishes 15 files totalling 75,120,759 bytes (75.1 MB). That
figure measures the published tarball instead of the working tree, and you can reproduce it:

```bash
npm view @mailwoman/neural-weights-en-us dist.unpackedSize dist.fileCount
```

Seven files account for most of the size:

| File                               | Bytes      | Size    | What it is                           |
| ---------------------------------- | ---------- | ------- | ------------------------------------ |
| `model.onnx`                       | 39,419,629 | 39.4 MB | The int8-quantized model, opset 17   |
| `fst-en-us.bin`                    | 21,757,160 | 21.8 MB | The gazetteer prior index            |
| `locality-surface-lexicon-v7.json` | 8,633,963  | 8.6 MB  | Locality surface forms               |
| `postcode-us.bin`                  | 1,904,322  | 1.9 MB  | The US postcode anchor table         |
| `tokenizer.model`                  | 1,632,289  | 1.6 MB  | The SentencePiece model              |
| `pair-index-us.bin`                | 1,341,264  | 1.3 MB  | The locality/region pair index       |
| `fst-street-morphology.bin`        | 327,516    | 0.3 MB  | The street-morphology emission prior |

The remaining eight files (the model card, the four soft-feed lexicons, two calibration tables and
the manifest) total 104,616 bytes.

The model card describes the model's structure:

| Property                | Value                                   |
| ----------------------- | --------------------------------------- |
| Encoder                 | 6 layers, 384 hidden, 6 attention heads |
| Maximum sequence length | 128 tokens                              |
| Tokenizer vocabulary    | 73,143 SentencePiece pieces             |
| Output labels           | 33 BIO labels over 16 component tags    |
| Quantization            | int8 dynamic, quantized from fp32       |

The gazetteer is a separate download and is not part of any npm package. The published build is
`gazetteer/2026-08-25b/candidate.db` at 2,880,921,600 bytes (2.88 GB), covering 244 countries. Run
`mailwoman doctor` for the current URL instead of copying one from a page.

## Locales

The tiers below are copied from
[`docs/engineering/SCOPE.mdx`](https://github.com/sister-software/mailwoman/blob/main/docs/engineering/SCOPE.mdx),
where they are maintained. Mailwoman claims support for a locale only where a coordinate-graded
evaluation exists for it. Having training data for a locale does not by itself mean it is supported.

| Tier | Locales                                                | What the tier means                                                                 |
| ---- | ------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| 1    | US, FR                                                 | First-class. Per-tag floors the release process enforces, plus real-world eval sets |
| 2    | IT, PT, PL, AT, CZ, DE, AU, BE, ES, NL, CH, HR, DK, FI | Trained, with a per-locale coordinate panel behind the claim                        |
| 3    | NO, SE                                                 | Trained exposure, thinly measured. Claims beyond the panel are unverified           |
| 4    | CZ, PL, SK, SI                                         | Covered by the diacritic tokenizer splice shipped in `5.1.0`                        |
| 5    | JP, KR, CN                                             | The character path, published in `9.3.0`. See the note below for what each carries  |

Tier 5 is one 49-label character-path head that serves all three languages, and the three have
different levels of support. Japanese has a 20,000-row coordinate board. Korean has a 20,000-row board
over 27 held-out 시군구 and resolves to the 시군구 through the candidate table's Korean names. No Korean
data finer than the county exists today. Chinese has 126 labeled rows in total, which is too few to
support a claim.

GB, IE, SE, HU and RO corpora from OpenStreetMap are waiting on an external constraint instead of
engineering work: the share-alike license review.

## Limits

The current parser and resolver handle cross-locale input within a defined boundary. The parser
preserves and labels the text it receives, and the gazetteer can connect multilingual aliases and
established transliterations to one place identity. Mailwoman does not currently translate arbitrary
address strings. See [Cross-locale queries](./knowledge-base/localization/cross-locale-queries.mdx) for
the behavior of English-written Japanese addresses, kana-written US addresses, and unsupported
scripts.

Each row states the limit and the next step. Where a limit is an off-by-default setting and not a
missing feature, the next step turns it on.

| Limit                                                                                      | Next step                                                                                                          |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| Geocoding requires the gazetteer database. Parsing does not.                               | Download it once: `mailwoman data pull candidate`. `mailwoman doctor` prints that command and the variable to set. |
| Confidence scores are uncalibrated by default. The shipped model is under-confident.       | Wire `createCalibrator` from `@mailwoman/core/decoder` with the bundled `calibration.json`.                        |
| The resolver stage has no default. `createRuntimePipeline` parses only until you pass one. | Pass `resolver:` a `createWOFResolver` instance backed by `@mailwoman/resolver-wof-sqlite`.                        |
| One classifier instance loads one locale.                                                  | Construct one `NeuralAddressClassifier` per locale you serve.                                                      |
| Korean resolves no finer than the 시군구. No sub-county register has been adopted.         | None today. The constraint is data coverage rather than engineering.                                               |
| The locale hint reports a single BCP-47 tag, so Han-script input is labeled `ja-JP`.       | Read the resolved country rather than the hint's locale when the input is Han-script.                              |
| Cross-language resolution requires a recorded gazetteer alias or transliteration.          | Supply the expected locale and keep unresolved results visible; arbitrary translation is not performed.            |

## Rationale

With a shared version number, one number tells you which release you have across every package,
including the weights. The drawback is that the release number says nothing about the model. Most
releases change only code, so `10.0.0`, `9.4.0` and `9.2.0` all ship the same model `9.1.0`. The model
card inside each weights package records the model's version history.

The gazetteer is distributed outside npm because it is 2.88 GB. A registry tarball of that size would
be downloaded by every consumer, including those who want only the parser.

## See also

- [Your first ten minutes](./get-started/ten-minute-trial.mdx) — running `doctor` against your own install.
- [Security and compliance](../about/security-and-compliance.mdx) — the network boundary, licensing, and SBOMs.
- [Support](./support.mdx) — where to report something this page got wrong.
