---
sidebar_position: 1
title: Status
description: What ships today, what's behind a flag, and what's still experimental.
---

# Status

This page is the single source of truth for what works in Mailwoman right now.

:::info[Verified as of 2026-07-14 — release 6.0.0]
The current npm release is **6.0.0** (`npm view mailwoman version`; `git tag` confirms `v6.0.0` at
`e4957876`), a breaking, code-only major — the Hono API-surface cutover. All four HTTP surfaces
(the native `mailwoman serve` plus the photon/nominatim/libpostal drop-ins) now run on one shared
Hono stack, and the express server is deleted in full with no compatibility shim. See
[Releases & capabilities](./releases.mdx) for the row.

Its **weights are the 5.9.0 line unchanged** (`v2.4.1-fr-nsplice-ft` — no training commits landed
between `v5.10.1` and `v6.0.0`, and the onnx 1.22.0 export-toolchain bump was re-verified
byte-identical against the shipped `model.onnx`). 5.9.0's model headline was **the principled
#444 FR-diacritic fix**: a training-free vocab splice had regressed FR street parsing (accent
mangle rate 23.5% → 26.4%) because the merged diacritic pieces inherited a strong "ignore me"
signal from their component pieces with no gradient of their own to move off it; a targeted
fine-tune gave those pieces gradient, bringing the mangle rate to **17.7%** and holding
US z=+0.29 / FR z=+0.90 on the held-out battery, gate PASS 17/17.

**The per-tag tables below are frozen at the 2026-07-02 full re-score of the earlier 5.0.0/v4.15.0
line** (the #885 measurement re-anchor). Five model releases have shipped since without a fresh
full re-score (5.1.0 → 5.4.0, then 5.9.0); their measured deltas are narrated release-by-release —
not folded into a new scorecard — on [Releases & capabilities](./releases.mdx), which is the
current per-release source of truth. Treat the [parity
scorecard](./evals/competitive-parity/parity-scorecard-2026-07-02.md) below as a historical
baseline, not the shipped model's live per-tag truth.
:::

## Packages

All publishable workspaces release in lockstep; the current npm version is **6.0.0**.

| Package                                                                                       | Description                                                         |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| `mailwoman`                                                                                   | CLI + high-level `AddressParser` + runtime pipeline factory         |
| `@mailwoman/core`                                                                             | Tokenization, classification, solver, decoder, policy registry      |
| `@mailwoman/codex`                                                                            | Per-address-system postal reference data (USPS suffixes, ZIP types) |
| `@mailwoman/classifiers`                                                                      | Rule-based classifiers                                              |
| `@mailwoman/neural`                                                                           | ONNX runtime + SentencePiece tokenizer + anchor inference           |
| `@mailwoman/neural-weights-en-us` / `-fr-fr`                                                  | Trained model bundles                                               |
| `@mailwoman/normalize` / `query-shape` / `locale-gate` / `kind-classifier` / `phrase-grouper` | Pipeline stages 1–2.7                                               |
| `@mailwoman/resolver-wof-sqlite` / `-wasm`                                                    | WOF gazetteer resolver (Node SQLite / browser WASM)                 |
| `@mailwoman/neural-web`                                                                       | Browser-side neural classifier (onnxruntime-web)                    |

## Model weights (the 5.9.0 line, carried unchanged into 6.0.0)

| Property             | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Lineage**          | `v2.4.1-fr-nsplice-ft` @ step 12k — an init_from FINE-TUNE of `v2.3.0-nl-postcode` (v5.4.0) on the `v0.8.0-fr-nsplice` spliced tokenizer, which itself built on `v2.2.0-full-family` (v5.3.0, the FR street-fragmentation fix) and the `v0.6.0-bsplice`/`v0.7.1-nsplice` diacritic vocab splices (v5.1.0/v5.2.0). Gives the merged FR diacritic pieces gradient after a training-free mean-init splice was falsified for regressing FR streets ([#1047](https://github.com/sister-software/mailwoman/pull/1047)) |
| **Architecture**     | 33.9M params — 6-layer encoder, 384 hidden, 6 heads, 66,319-piece SentencePiece embedding (expanded from the 48K-vocab v4.15.0 line by the diacritic + FR-nsplice vocab splices)                                                                                                                                                                                                                                                                                                                                 |
| **Label vocabulary** | 33 BIO labels over 16 component tags (incl. `unit`, `po_box`, `street_prefix`/`_suffix`, `intersection_a/b`) — unchanged                                                                                                                                                                                                                                                                                                                                                                                         |
| **Training corpus**  | `corpus-v0.10.1-nl-postcode` = 699 shards (the v0.10.0 boundary-family base + the synth-nl-postcode shard, [#924](https://github.com/sister-software/mailwoman/issues/924)) · tokenizer `v0.8.0-fr-nsplice` (md5 `04995524…`)                                                                                                                                                                                                                                                                                    |
| **Anchors**          | Postcode anchor (PCB1) + gazetteer soft anchor (country candidate-tags), both live at inference                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Conventions**      | Locale head exported (`locale_logits`) → conventions mask (fr row) + punctuation-gap span bridge — BOTH required ship config (`addressSystemConventions:'auto'`, `bridgePunctuationGaps:true`)                                                                                                                                                                                                                                                                                                                   |
| **Decode**           | Viterbi with frozen BIO structural mask (CRF at inference; CE-only training)                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Size**             | 36.8 MB int8 ONNX (146.6 MB fp32) · opset 17 · max sequence 128                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

### Per-tag health (summary)

The full per-tag table — two lenses, golden-dev and real-OOD — lives in the
[parity scorecard](./evals/competitive-parity/parity-scorecard-2026-07-02.md). It is frozen at the
2026-07-02 re-score of the 5.0.0/v4.15.0 line, one model generation behind the shipped 5.9.0 line
above — read it as a historical baseline, not this model's live numbers. The sortable summary:

import { F1ScoreTable } from "@site/src/components/F1ScoreTable/F1ScoreTable"

<F1ScoreTable />

:::tip[Click to sort · Hover for context]
Click any column header to sort the table by that metric. Hover over any F1 score cell to see an
explanation of what that value means in the context of the eval set it was measured on.
:::

The trend, in words:

- **Healthy (2026-07-02 re-score):** postcode (95.0/99.3 US/FR — both sitting exactly at their
  revised gate floors), house_number (90.7/98.1), venue-US (89.1), street (82.3 US, the best it
  has measured), fr.region recovered 16.2 → 48.4 by the multi-locale work.
- **Fixed by the parity campaign:** `unit` 0 → 92.3 on real-OOD designators (the 4.1.0 headline).
- **Shipped by the consolidation (v4.2.0):** locality +12.8, region +10.7, country 0 → 89.8
  (gazetteer soft anchor), affixes now exist (0 → 64.9/48.8 at P≈100), DE 90.9.
- **Closed by the boundary consolidation (v4.4.0):** the scorecard's last empty rows — po_box
  0 → 89.1, cedex 0 → 96.1, intersections 0 → 100 (real-OOD, P ≥ 95) — and the v4.3.0 arena
  debt (perturb 64 → 72, now a gated floor). The span bridge ships as required decode config:
  dotted surfaces ("P.O. Box") are structurally unlabelable in the corpus format; the bridge is
  the containment ([ship gate](./evals/model-versions/2026-06-11-v4.4.0-ship-gate.md) tells the three-battery story).
- **Closed by the relabel (v4.3.0):** the affix ceiling was never capacity — it was a 1,039:1
  label contradiction in the training mix ([#492](https://github.com/sister-software/mailwoman/issues/492) →
  [#511](https://github.com/sister-software/mailwoman/issues/511)). Affixes now 93.6/96.6 (int8,
  real-OOD; 92.2/90.3 on the 193-row NAD-native eval where v4.2.0 scored 18.2/8.9). Plus the
  first conventions-layer slice: the model's locale head is exported and OBEYED (fr mask).
- **Formerly "open gaps," now closed:** the two rows this section used to flag are both resolved.
  FR venue/region ([#330](https://github.com/sister-software/mailwoman/issues/330)) was a
  measurement artifact — the model's real FR venue F1 is 89.1+ and région 99.6% in-distribution,
  not the 0%/19% a synthetic multi-script eval implied. Intersections
  ([#487](https://github.com/sister-software/mailwoman/issues/487)) were fixed by the v4.4.0
  real-TIGER intersection shard (0 → 100/100 real, 94.9/96.1 VT golden).

Capability-arena truth (whole-parse, vs the v0 rules system): rules still win on
clean/canonical input, the neural parser wins decisively on noisy/degraded input (+21pp),
both are weak on rare edge formats. An
[arbitration layer (#478)](https://github.com/sister-software/mailwoman/issues/478) was built to
make the pipeline take the better of both per component, but its promotion was falsified by the
coordinate gate ([#685](https://github.com/sister-software/mailwoman/issues/685) — arena +122
label-wins cost coord p50 3.3 → 1069 km and broke the street+house_number geocode precondition
100% → 48%). It stays merged, default-off, research-only.

## Runtime pipeline — stage status

| Stage | Name             | Status     | Notes                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | ---------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | Normalize        | ✅ default | NFC, whitespace collapse, locale-aware case folding                                                                                                                                                                                                                                                                                                                         |
| 2     | Locale gate      | 🚩 opt-in  | Ships (`@mailwoman/locale-gate`); not wired as factory default                                                                                                                                                                                                                                                                                                              |
| 2.5   | Kind classifier  | ✅ default | Rule-based; fast-paths bare postcodes and single localities                                                                                                                                                                                                                                                                                                                 |
| 2.7   | Phrase grouper   | ✅ default | Rule-based span proposals before classification                                                                                                                                                                                                                                                                                                                             |
| 3     | Token classify   | ✅ default | Neural encoder + rule classifiers, hybrid via policy registry (per-component modes)                                                                                                                                                                                                                                                                                         |
| 4     | Sequence correct | ✅ default | Viterbi over the frozen BIO mask; postcode/unit label repairs                                                                                                                                                                                                                                                                                                               |
| 5     | Reconcile        | 🚩 opt-in  | Joint decoding with WOF concordance scoring. **Retired as the default** (#566): on the assembled pipeline it broke the street + house_number geocode precondition on 77–84% of clean US addresses and fixed 0%, so argmax is now the default decode. Code + A/B harnesses remain; opt back in via `jointReconcile: true`                                                    |
| 6     | Resolve          | 🚩 opt-in  | WOF SQLite gazetteer: admin/postcode-level candidates with coordinates, exact-match tiering, coordinate-first postcode disambiguation, opt-in `hierarchyCompletion`/`cityStateFallback`/`includeAncestors`, plus reverse geocoding (`mailwoman reverse` — bbox → PIP over `wof-polygons` → ancestor chain, [#484](https://github.com/sister-software/mailwoman/issues/484)) |

## Confidence

Every node carries a confidence score; pass a calibrator (isotonic, shipped per-locale in the
weights bundle as `calibration.json`) to make those scores honest probabilities — see
[Confidence calibration](./concepts/confidence-calibration.mdx) for what the numbers mean.

## Browser demo

The live demo at [mailwoman.sister.software/demo](https://mailwoman.sister.software/demo) runs the
int8 model (36.8 MB, the 5.9.0 line) via `@mailwoman/neural-web` plus a **byte-range global
gazetteer** — the FTS-free "candidate" table (~530 MB, queried over `sql.js-httpvfs`; a resolve
touches ~12 range fetches regardless of file size) with global admin coverage + **US + EU postcode
resolution** and a debounced FST place-autocomplete in the search box. US **street-level
(rooftop)** resolves nationwide — all 50 states + DC + VI — via per-state situs/interp shards
(124.9M address points, [#735](https://github.com/sister-software/mailwoman/issues/735)); FR
rooftops resolve via the hosted BAN situs shard with a street-tier fallback (shipped 5.10.0, see
[Releases & capabilities](./releases.mdx)). Artifacts are served byte-range from R2. The demo
calls the classifier's argmax path directly; joint reconcile is not active in the demo.

## Model line (July 2026)

The shipped weights are the **5.9.0 line** (`v2.4.1-fr-nsplice-ft`, carried unchanged through
5.10.x and into 6.0.0 — 6.0.0 itself is a code-only major, the Hono API cutover; no training
commits landed in that window). Five model releases shipped between the 5.0.0/v4.15.0 line
(documented in past versions of this page) and today, each held to the **shipped assembled
coordinate or a targeted repro**, not raw label-F1: v5.1.0 (`v0.6.0-bsplice` tokenizer splice)
fixed Slavic diacritic fragmentation — CZ/PL/SK/SI wrong-city roughly halved, US byte-identical;
v5.2.0 (`v0.7.1-nsplice`) extended the splice to Nordic diacritics (SE resolve 83.8 → 90.2%);
v5.3.0 (`v2.2.0-full-family`, from scratch) fixed the FR street fragmentation the v5.2.0 splice had
shipped ungated (bare 16/40 → 34/40, anchored 13/40 → 37/40); v5.4.0 (`v2.3.0-nl-postcode`,
fine-tune) fixed a Dutch digits-first postcode misread that was pulling `1012 LG Amsterdam` into
the US situs tier; v5.9.0 (`v2.4.1-fr-nsplice-ft`, fine-tune) gave the merged FR diacritic pieces
gradient after a training-free vocab splice was falsified for regressing FR streets, cutting the
accent mangle rate 23.5% → 17.7%. Full per-release evidence lives on
[Releases & capabilities](./releases.mdx), which this page points to for anything shipped after
the last full re-score. The ordered work queue to the full geocoder is
[epic #488](https://github.com/sister-software/mailwoman/issues/488).

## What is not supported today

- Multi-locale ensemble (en-US + fr-FR weights in the same runtime).
- Korean addresses (no open data path). Japanese addresses **are** supported via the
  postcode-route resolver (Geographic Rule Engine).

Three items that used to live on this list have shipped and are covered elsewhere on this page:
nationwide US rooftop geocoding (all 50 states + DC + VI, see **Browser demo** above,
[#735](https://github.com/sister-software/mailwoman/issues/735)), reverse geocoding (`mailwoman
reverse`, see the **Runtime pipeline** table above, [#484](https://github.com/sister-software/mailwoman/issues/484)),
and the production-hardened server API (batch, observability, `RemoteResolver`, versioned data
switchover — shipped v4.7.0, [#485](https://github.com/sister-software/mailwoman/issues/485); the
6.0.0 Hono cutover retired the last express-era code path, see
[Releases & capabilities](./releases.mdx)).
