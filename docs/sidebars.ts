import type { SidebarsConfig } from "@docusaurus/plugin-content-docs"

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

// One sidebar per top-level section. The docs sub-header (src/components/DocsSubHeader)
// is the section switcher, so the sidebar no longer wraps each section in a collapsible
// category — every section's docs sit at the top level of their own sidebar, one
// hierarchy level shallower. The sidebar ids here are the single source of truth for
// the switcher (see src/components/DocsSubHeader/sections.ts, which must list the same
// ids in display order). Each doc belongs to exactly one sidebar, so Docusaurus shows
// the right one automatically.
const sidebars: SidebarsConfig = {
	// Explicit (not autogenerated): these live at the articles/ root alongside orphans
	// like sotm-2026-talk-proposal that should stay out of the nav.
	startHere: ["status", "releases", "getting-started", "api"],
	understanding: [
		{
			type: "autogenerated",
			dirName: "understanding",
		},
	],
	concepts: [
		{
			type: "autogenerated",
			dirName: "concepts",
		},
	],
	recipes: [
		{
			type: "autogenerated",
			dirName: "recipes",
		},
	],
	plan: [
		{
			type: "autogenerated",
			dirName: "plan",
		},
	],
	evals: [
		{
			type: "autogenerated",
			dirName: "evals",
		},
	],
	retrospectives: [
		{
			type: "autogenerated",
			dirName: "retrospectives",
		},
	],
	licensing: [
		{
			type: "autogenerated",
			dirName: "licensing",
		},
	],
}

export default sidebars
