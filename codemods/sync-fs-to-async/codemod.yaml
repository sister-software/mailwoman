schema_version: "1.0"

name: "sync-fs-to-async"
version: "0.1.0"
description: "Rewrite a synchronous node:fs call that stands inside an async function to the @mailwoman/core/fs async helper"
author: "Sister Software"
license: "AGPL-3.0"


targets:
  # Keep this aligned with the files matched in workflow.yaml.
  languages: ["typescript", "tsx"]

keywords: ["fs", "async", "event-loop", "node", "mailwoman"]

registry:
  access: "public"
  visibility: "public"

capabilities: []

workflows:
  - name: main
    default: true
    path: workflow.yaml
