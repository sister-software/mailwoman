/**
 * @copyright Sister Software
 * @license AGPL-3.0
 * @author Teffen Ellis, et al.
 *
 * @deprecated Moved to `mailwoman/test-kit` (the `./test-kit` subpath) — `sdk/` submodules mean
 *   data acquisition. This shim keeps the published `./sdk/test` subpath alive; remove at the next
 *   major (bundle with the #875 batch).
 */

export * from "../../test-kit/index.ts"
