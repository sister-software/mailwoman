{
	"name": "neural-weights-en-gb",
	"version": "9.0.0",
	"$comment_661": "6.6.1 (2026-07-24) is an ARTIFACT + DECODE bundle revision on an UNCHANGED model: same v385 encoder bytes as 6.6.0 (see base_model), pair-index-gb.bin re-cut at δ=10 (was 5.0 — calibrated for a candidate checkpoint that was never promoted, provably inert on the shipped model: 0/69), consumed by @mailwoman/neural ≥7.7.0's probe CHAIN (probeMode \"auto\": segment path on comma'd input, anchored-adjacent path on comma-free). The 6.7.0 staged-candidate lineage referenced by 6.6.0's card is CLOSED (stop rule, five falsified training mechanisms — see the base card's phase); this capability is carried entirely by the decode-time prior on the shipped model.",
	"$comment_new_card": "Card became overlay-LOCAL in the 7.6.0 cycle (previously the #1177 card-less-overlay fallback to the base card). `labels`/`requires`/`architecture`/`components_supported` are duplicated VERBATIM from the base card so decode behavior matches the base exactly — keep in lockstep on every base ship.",
	"model_lineage": "Data-only overlay. Ships NO model.onnx/tokenizer.model of its own — resolves both from `@mailwoman/neural-weights-en-us` via `mailwoman.baseWeights` (byte-identical artifact; one encoder serves both locales). What this package ships: `pair-index-gb.bin` (PIX1 placetype-pair retrieval index, built from HM Land Registry PPD tuples, δ=10 as of 6.6.1) and `postcode-gb.bin` (PCB1, Code-Point Open). The postcode-anchor channel resolves ON for en-gb loads — the encoder's GB anchor slot is trained. The interim era when this overlay shipped no postcode binary against an untrained slot is recorded in `gb_artifacts.no_postcode_bin`.",
	"phase": "SHIPPED (6.6.1, 2026-07-24) — GB dependent_locality goes LIVE via the decode-time placetype-pair prior on the untouched shipped v385 base. The Option-A δ-sweep (task-8 report, 2026-07-23) showed v385's dead-classifier deficit for the tag is large but UNIFORM (~7.0 logits mean), so δ=10 clears the entire 69-row golden board at once: 69/69 emit, 69/69 tag-correct, 0/51 golden false positives — exceeding every trained candidate the resurrection arc produced. Comma-free coverage comes from @mailwoman/neural 7.7.0's anchored-adjacent probe path (50/69 emit, 94% tag-correct, venue floor 0.062%). The training-side resurrection lineage (v3.10–v3.13) is CLOSED: five mechanisms falsified, stop rule executed three times; model-side redesign is unhurried research, not this capability's dependency.",
	"license": "AGPL-3.0-only OR LicenseRef-Commercial",
	"locale": "en-gb",
	"base_model": {
		"package": "@mailwoman/neural-weights-en-us",
		"version": "6.6.3",
		"$comment": "Same model BYTES as 6.6.0 (v385, model.onnx md5 700f3cf4c08f31536f0bc053c281b19e) — the bundle version moves for the artifact re-cut, the encoder does not.",
		"resolution": "mailwoman.baseWeights in package.json — resolveWeights/resolveFromPackageDir resolve model.onnx + tokenizer.model from the base package dir (source suffixed \"+base\"); this card's own labels/requires/architecture below are a verbatim copy of the base's, not re-derived at runtime — keep them in lockstep on every base ship (same discipline as neural-weights-en-gb/scripts/link-dev-weights.ts's DEFAULT_MODEL/DEFAULT_TOKENIZER md5 lockstep comment)."
	},
	"gb_artifacts": {
		"no_postcode_bin": {
			"$comment": "INTERIM MITIGATION, 2026-08-05 (#1467). This overlay used to ship `postcode-gb.bin` (PCB1, outward-code granularity, built from the WOF GB postcode shard). It no longer does, and `release.config.json`'s softFeed.postcodeDBByCountry no longer carries a `gb` entry, so nothing in the repo or in CI materializes one. en-nz is the precedent: no postcode binary, anchor channel resolves OFF, tolerant-loader warns once and the parse still works.",
			"why": "The encoder's GB anchor slot received ZERO training gradient. `neural/anchor-inference.ts`'s LOCALE_ORDER is [US, FR, DE, CA, GB, JP, ES, IT, NL]; GB is slot 4. Every training recipe under corpus-python/src/mailwoman_train/configs/ sets anchor_lookup_path to the SAME file, /data/anchor/pilot-anchor-lookup.json — 67,708 keys, ZERO letter-bearing, covering exactly US (42,312) / DE (29,694) / FR (27,119). No training example ever put a non-zero value in slot 4. `postcode-gb.bin` is the only artifact that does, and it fires on 106/120 gb-golden rows (every row carrying a postcode), so shipping it fed an untrained input direction on essentially every GB parse.",
			"measured": "gb-golden postcode board, en-gb overlay, three registers (as-written / lowercase / UPPERCASE), exact match: 294/318 anchor-ON vs 318/318 anchor-OFF. dependent_locality board: 207/207 both arms (69/69 per register). Comma-STRIPPED dependent_locality: 201/207 anchor-ON vs 198/207 anchor-OFF — one row regresses ('Goulbourne Road St Georges Telford TF2 9LE', dependent_locality clipped to 'St'), which is the accepted cost of the +24 postcode rows. Receipts: docs/records/evals/2026-08-05-en-gb-anchor-off.md.",
			"reversal": "The artifact stays BUILDABLE — the WOF GB shard ($MAILWOMAN_DATA_ROOT/wof/postalcode-gb.db) and `mailwoman gazetteer postcode-binary --out neural-weights-en-gb --locale GB:<shard>` are both untouched. Re-adding the binary WITHOUT a retrain that feeds slot 4 is a regression; `neural/test/weights.test.ts` pins anchorLookupPath undefined for en-gb so it fails loudly. The retrain prescription is filed."
		},
		"pair_index_gb_bin": {
			"format": "PIX1 placetype-pair index (placetype-pair-prior arc, Task 3) — a retrieval-augmented (child, parent) dependent_locality prior, e.g. (\"Fishburn\", \"Stockton-on-Tees\")",
			"source": "HM Land Registry Price Paid Data (PPD) tuples CSV, $MAILWOMAN_DATA_ROOT/ppd/2026-07-22/gb-tuples.csv, md5 dbd675bdc970ea60f96f0a470429a128, 25,674,048 rows",
			"pairs": 30825,
			"bytes": 793449,
			"md5": "(staged at release time — see $comment_md5)",
			"$comment_pix2": "PIX2 (schema 3, 2026-08-04): every record now carries the PARENT's ComponentTag alongside the child's — +1 byte per pair, pair COUNT unchanged. Schema 1 and 2 binaries are refused; the artifact must be rebuilt.",
			"delta_calibration": "parentDelta=5 (whole-edge parent bias, #46 — the record's own parentTag gets +5 over the parent window). δ=5 is the smallest magnitude that saturates bar B-2's brooklyn-class sub-board and is flat through 20, at 0.00% parent-side FP on B-3: docs/records/evals/2026-08-04-pix1-whole-edge-verdict.md. GB's own shipped board moved 66/69 → 69/69 whole-edge with it on. δ=10 (Option-A sweep on the SHIPPED v385, task-8 report 2026-07-23 — supersedes 6.6.0's δ=5.0, which was calibrated for the never-promoted feed-8k candidate and is provably inert on v385: 0/69). v385's dead-row deficit is uniform (~7.0 logits mean, 4.95 min), so the emit/correct curves and the venue-FP curve all saturate together at δ=10 (δ=12/15 byte-flat). δ remains a recall/FP dial in general — the sweep table lives in eval.delta_sweep_v385 below.",
			"probe_mode": "\"auto\" (@mailwoman/neural ≥7.7.0 default): the segment path on comma'd input (whole comma-delimited segments — byte-identical to the previous \"segment\" default by construction AND re-measurement), the ANCHORED-ADJACENT path on comma-free input (parent window anchored immediately left of the postcode-shaped span or string-final; child window immediately adjacent, left-maximal; bias on the child span only). Window mode (any-to-any geometry) remains opt-in-only: 79% venue-confound FP at δ=10 — the geometry, not the probing, is its flaw (see the 2026-07-24 brainstorm-response doc).",
			"country_gate": "hard-gated — the prior only fires when the resolved locale's country matches the index header's country (gb). Never crosses into en-us parses (see neural/test/weights.test.ts).",
			"not_committed": "derived artifact, .gitignore'd (same posture as the anchor/country lexicons) — rebuilt via `mailwoman gazetteer pair-index --out neural-weights-en-gb --country gb --source <ppd-csv> --delta 10`",
			"transition_beta": 5,
			"$comment_663": "6.6.3 re-cut: header gains transitionBeta 5 — on a pair hit, a position-scoped bonus on the transition into B-dependent_locality at the child's first piece (neural ≥7.8.1). Fixes the comma-free path-fusion class: 55→63/69 emit, 95.2% tag-correct. Title-preposition suppression ({at, of} predecessors) withholds the bonus inside lexicalized venue names (the 'New Inn at Hoff' class) — venue-anchored FP restored to the exact pre-beta 4/6500. Rebuild: gazetteer pair-index --delta 10 --transition-beta 5.",
			"$comment_md5": "REPRODUCIBILITY: a PIX1 header embeds `buildDate`, so a rebuild from identical sources produces different bytes and a different md5. This md5 therefore identifies the artifact STAGED TO HUGGING FACE for this release — it is a provenance record of the shipped bytes, NOT a check a local rebuild can reproduce. A mismatch after rebuilding is expected and is not corruption; verify pair COUNT and header delta/beta instead.",
			"$comment_r7": "Composition as shipped (this block had been STALE since R2, still reporting the original 19,209/457,862 PPD-only build through three increments): 19,209 PPD pairs + 917 secondary = 15 WOF boroughs (R2) + 815 London wards/neighbourhoods (R3/R4b, data/gazetteer/london-pairs-v2.jsonl) + 87 Northern Ireland (R7, data/gazetteer/ni-pairs-v1.jsonl). Rebuild: gazetteer pair-index --country gb --delta 10 --transition-beta 5 --borough-db <wof/admin-global-priority.db> --pairs-jsonl <london-pairs-v2.jsonl>,<ni-pairs-v1.jsonl>",
			"$comment_r8": "Campaign R8 (2026-08-02) completes Great Britain: 19,209 PPD + 11,625 secondary = 15 WOF boroughs (R2) + 815 London (R3/R4b) + 87 Northern Ireland (R7) + 10,708 Scotland/Wales/England (data/gazetteer/gb-regions-v1.jsonl). Welsh civil-parish parents have their administrative suffix stripped — the register names the parish 'Pontypridd Community' but an address says 'Pontypridd'; 1,545 stripped. 579 England rows carrying an EMPTY parent are dropped.",
			"$comment_currency": "CURRENCY FILTER (2026-08-02): both ends of every pair are now required LIVE — `is_current != 0 AND is_deprecated = 0`. The WOF ingest hard-rejects `wof:superseded_by` records but RECORDS `edtf:deprecated` and `mz:is_current` as columns rather than rejecting them, so filtering is the reader's job and this extraction was not doing it. Pair count 30834 → 30825."
		}
	},
	"architecture": {
		"$comment": "Verbatim copy of the base card's architecture — same encoder, no GB-specific change.",
		"hidden_size": 384,
		"num_hidden_layers": 6,
		"num_attention_heads": 6,
		"intermediate_size": 1536,
		"max_position_embeddings": 128,
		"vocab_size": 73143,
		"num_labels": 33,
		"crf_at_training": false,
		"crf_at_inference": true,
		"phrase_priors": true
	},
	"requires": {
		"$comment": "Copy of the base card's requires block — same trained ship-config, same fail-closed contract via createScorer. Keep in lockstep with neural-weights-en-us/model-card.json on every base ship, EXCEPT conventions.mode, which is deliberately locale-pinned (see conventions.$comment), and anchor, which now carries a deliberate GB deviation (see anchor.$comment).",
		"anchor": {
			"required": true,
			"$comment": "9.0.0 (ROAD_TO_V9 A4): the anchor channel is BACK ON for GB — v4.2.0 trained slot 4 (Fisher 11.28% of anchor-projection mass; gb-golden 318/318 with the channel fed vs the #1467-era 294/318 with it fed-but-untrained). span_mode 'shaped' is load-bearing: postcode-gb.bin's 1,746,976 unit keys are space-stripped upper and unreachable by the alnum-run scan. The 2026-08-05 anchor-OFF mitigation record stays in gb_artifacts for history.",
			"span_mode": "shaped"
		},
		"gazetteer": {
			"required": true
		},
		"country": {
			"required": true
		},
		"conventions": {
			"required": true,
			"mode": "gb",
			"$comment": "PINNED to gb, not auto (#1275, 2026-07-24): auto reads the locale head, and the head under-detects GB on this overlay (US=0.780 below the 0.8 bar on the comma'd repro row, US=0.986 comma-free) — so the codex gb conventions row (the postcode snap-repair gate) was unreachable and the clip class (SK11 9PD → 'K11 9PD') stood at 44/106 on the gb-golden postcode rows. This bundle exists solely for GB parses, so the system is known a priori — pinning is the ship-default the diagnosis prescribed. Measured: exact 26 → 83, clips 44 → 0."
		},
		"bridge": {
			"required": false
		},
		"suppress_gazetteer_near_postcode": true,
		"street_type": {
			"required": true,
			"feature_dim": 1,
			"slots": ["street_type"],
			"lexicon": "street-type-lexicon-v3.json"
		},
		"locality_surface": {
			"required": true,
			"feature_dim": 2,
			"slots": ["locality", "locality_homograph"],
			"lexicon": "locality-surface-lexicon-v7.json",
			"$comment_1511": "RESTORED 2026-08-06. Both evidence channels were MISSING from this block for the whole life of the overlay, while the block's own header claimed to be a verbatim copy of the base card's — so en-gb ran the Option-A bundle OFF on a model trained WITH it (#1511). `lexicon` names the generation each channel resolves (#1510): resolveWeights and neural-weights-en-gb/scripts/link-dev-weights.ts both read these fields rather than a hard-coded filename, so a card bump moves the artifact. Keep in lockstep with neural-weights-en-us/model-card.json on every base ship."
		}
	},
	"labels": [
		"O",
		"B-country",
		"I-country",
		"B-region",
		"I-region",
		"B-locality",
		"I-locality",
		"B-dependent_locality",
		"I-dependent_locality",
		"B-postcode",
		"I-postcode",
		"B-subregion",
		"I-subregion",
		"B-cedex",
		"I-cedex",
		"B-venue",
		"I-venue",
		"B-street",
		"I-street",
		"B-house_number",
		"I-house_number",
		"B-street_prefix",
		"I-street_prefix",
		"B-street_suffix",
		"I-street_suffix",
		"B-unit",
		"I-unit",
		"B-po_box",
		"I-po_box",
		"B-intersection_a",
		"I-intersection_a",
		"B-intersection_b",
		"I-intersection_b"
	],
	"components_supported": [
		"country",
		"region",
		"locality",
		"dependent_locality",
		"postcode",
		"subregion",
		"cedex",
		"venue",
		"street",
		"house_number",
		"street_prefix",
		"street_suffix",
		"unit",
		"po_box",
		"intersection_a",
		"intersection_b"
	],
	"notes": "6.6.1 is the release where GB dependent_locality actually WORKS on what ships: the prior alone on the untouched v385 base. History for readers of 6.6.0's card: that card's numbers (69/69 @ δ=5 etc.) were measured on a staged candidate checkpoint (feed-8k) that failed the Gauntlet's metamorphic layer and was never promoted — on the SHIPPED model, δ=5 did nothing (0/69). The Option-A sweep found v385's deficit uniform and δ=10 sufficient; the anchored-adjacent probe path (neural 7.7.0) then added comma-free coverage at a 0.062% venue floor, superseding the comma-scoped-v1 framing before it shipped. The two comma'd-board misses of earlier cards are gone (69/69 tag-correct at δ=10); the comma-free board's 19 misses are 17 Viterbi path-level fusions (bias wins per-token, the global path routes through a fused street/locality run — a model-side ceiling the prior cannot cross) + 2 non-adjacent-geometry rows. Venue-confound floors documented honestly: 3.338% on the comma'd board (segment path, 217/6500 — the street-verbatim-child residual), 0.062% on the comma-free board (anchored path, 4/6500). All numbers: .superpowers/sdd/task-8-report.md §§ 'Option A', 'Ship battery', 'Experiment 0', 'v1.1 anchored-mode battery'; design record: docs/superpowers/plans/2026-07-24-pair-prior-comma-scope-{KIMI_HANDOFF,BRAINSTORM_RESPONSE}.md.",
	"eval": {
		"$comment": "All rows measured on the SHIPPED v385 base + this package's δ=10 pair-index via @mailwoman/neural 7.7.0's auto probe chain (2026-07-24 battery, pre-registered bars).",
		"gb_golden_dependent_locality": {
			"n": 69,
			"prior_on_at_delta_10": {
				"emit": 69,
				"tag_correct": 69,
				"tag_correct_pct": 100.0
			},
			"source": "mailwoman/eval-harness/fixtures/gb-golden.jsonl"
		},
		"gb_golden_comma_stripped": {
			"n": 69,
			"auto_chain_delta_10_beta_5": {
				"emit": 63,
				"tag_correct": 60,
				"tag_correct_pct": 95.2
			},
			"note": "transition-beta (6.6.3): 8 net-new recoveries over the anchor-channel baseline; residual 6 rows named in the task-8 report (2 multi-piece-continuation, unreachable at any β)."
		},
		"gb_golden_no_dependent_locality_fp": {
			"n": 51,
			"fp": 0
		},
		"venue_confound_fp": {
			"board": "gb-venue-confound-full.jsonl, 6,500 rows",
			"delta_star": 10,
			"segment_path_comma_present": {
				"fp": "217/6500",
				"fp_pct": 3.338
			},
			"anchored_path_comma_free": {
				"fp": "4/6500",
				"fp_pct": 0.062
			},
			"window_mode_reference": {
				"fp_pct": 78.969,
				"note": "opt-in-only mode, documented for contrast — the any-to-any pair geometry is the flaw"
			}
		},
		"delta_sweep_v385": {
			"$comment": "Option-A sweep, segment mode, comma'd gb-golden board (emit/tag-correct of 69) + venue-confound FP%.",
			"5": {
				"emit": 0,
				"tag_correct": 0,
				"fp_pct": 0.385
			},
			"6": {
				"emit": 6,
				"tag_correct": 5,
				"fp_pct": 1.4
			},
			"7": {
				"emit": 35,
				"tag_correct": 32,
				"fp_pct": 2.477
			},
			"8": {
				"emit": 68,
				"tag_correct": 64,
				"fp_pct": 3.308
			},
			"10_selected": {
				"emit": 69,
				"tag_correct": 69,
				"fp_pct": 3.338
			},
			"12": {
				"emit": 69,
				"tag_correct": 69,
				"fp_pct": 3.338
			},
			"15": {
				"emit": 69,
				"tag_correct": 69,
				"fp_pct": 3.338
			}
		},
		"invariance_suite": "zero NEW violation classes vs the v385 baseline with the auto chain active; 138 INVARIANT pairs vs segment-only's 134 (the chain improves the profile).",
		"presets_and_golden": "6 demo presets + golden us/fr byte-identical to v385 (0/4,206 rows differ) — the en-gb country gate + the chain's comma'd/comma-free dispatch leave non-GB parses untouched by construction and by measurement.",
		"gauntlet": "the base model is the SHIPPED v385 — the gauntlet's shipped-baseline status is unchanged by this data-only bundle."
	},
	"format": {
		"$comment": "Model/tokenizer format is the base's — see neural-weights-en-us/model-card.json. This overlay's own artifacts:",
		"pair_index_gb_bin": "PIX1 binary (schema 3), 793,449 bytes, 30,825 pairs, δ=10, β=5, parentDelta=5"
	},
	"files": {
		"model_card": "model-card.json",
		"$comment_postcode_anchor": "RESTORED 2026-08-06 (9.0.0): postcode-gb.bin returns — Code-Point Open (OGL v3.0), 1,749,839 keys (1,746,976 units + 2,863 outward districts), built by `mailwoman gazetteer postcode-binary` from postalcode-gb-codepoint.db. The 2026-08-05 absence (#1467) was the untrained-slot mitigation; v4.2.0 is the cure.",
		"postcode_anchor": "postcode-gb.bin",
		"pair_index": "pair-index-gb.bin",
		"gazetteer_lexicon": "anchor-lexicon-v1.json",
		"country_lexicon": "country-surface-lexicon-v1.json"
	},
	"$comment_662": "6.6.2 (2026-07-24): version lockstep with the base bundle (en-nz overlay joins; the (x,x) identity-pair segment rule lands in neural 7.8.0 — GB is structurally inert to it, 0 identity pairs in this index, re-measured exact). GB artifacts and eval numbers unchanged from 6.6.1."
}
