# Present so `npm publish` honors package.json's `files` whitelist instead of
# falling back to .gitignore (which excludes the binaries — they're gitignored
# on purpose but MUST be packaged at publish time).
