{
	"name": "@mailwoman/neural-weights-de-de",
	"version": "7.0.0",
	"locale": "de-DE",
	"license": "AGPL-3.0",
	"requires": {
		"$comment": "Copy of the base card's requires block — same trained ship-config, same fail-closed contract via createScorer. This overlay shares the base model.onnx byte-for-byte, so the channels it was trained against are the base's; keep in lockstep with neural-weights-en-us/model-card.json on every base ship. Without this block the loader inferred the channels from the ONNX graph and resolved the evidence lexicons by legacy filename, which fed street_type by guess and left locality_surface OFF (v6 sought, v7 shipped); a consumer of the published tarball, which carried no lexicon at all, ran gazetteer, country and both evidence channels OFF against a model trained with them (#1668, #2115). Measured before shipping: locality_surface ON vs OFF, 0 of 49 board rows differ (DE 12, ES 25, IT 12).",
		"anchor": {
			"required": true,
			"span_mode": "shaped",
			"$comment": "span_mode 'shaped' is the 9.0.0 SHIP OBLIGATION (recipe header + ROAD_TO_V9 A2): the train painter keys postcodes as shape-detected spans, space-stripped upper ('SW1A 2AA' → 'SW1A2AA'); the default alnum-run scan can NEVER produce that key, so omitting this leaves GB/NL postcodes unanchored at serve — the mirror image of #1467. createScorer fails closed on a shaped-declaring card paired with an artifact whose keys the scan cannot reach."
		},
		"gazetteer": {
			"required": true
		},
		"country": {
			"required": true
		},
		"conventions": {
			"required": true,
			"mode": "auto"
		},
		"bridge": {
			"required": false
		},
		"suppress_gazetteer_near_postcode": true,
		"street_type": {
			"required": true,
			"feature_dim": 1,
			"slots": ["street_type"],
			"lexicon": "street-type-lexicon-v3.json"
		},
		"locality_surface": {
			"required": true,
			"feature_dim": 2,
			"slots": ["locality", "locality_homograph"],
			"lexicon": "locality-surface-lexicon-v7.json"
		}
	},
	"$comment": "Data-only overlay. Shares model.onnx + tokenizer.model with @mailwoman/neural-weights-en-us via mailwoman.baseWeights — this package ships ONLY the DE placetype-pair index.",
	"de_artifacts": {
		"$comment": "Hierarchy campaign R9 (2026-08-02). The German instance of the dependent-locality prior.",
		"pair_index_de_bin": {
			"format": "PIX1 placetype-pair index — a retrieval-augmented (Ortsteil, Gemeinde) dependent_locality prior, e.g. (\"Nippes\", \"Köln\")",
			"source": "Who's On First admin hierarchy ($MAILWOMAN_DATA_ROOT/wof/admin-global-priority.db). Children: borough + neighbourhood (WOF's typing for Ortsteile/Stadtteile). Parents: locality, localadmin, borough. No postal register: Deutsche Post routes 'PLZ Stadt' and carries no Ortsteil field, so WOF is the source — the same call the US instance made and the opposite of France's, where BAN's lieu-dit field IS the postal line.",
			"pairs": 85603,
			"bytes": 2246525,
			"$comment_pix2": "PIX2 (schema 3, 2026-08-04): every record now carries the PARENT's ComponentTag alongside the child's — +1 byte per pair, pair COUNT unchanged. The recorded parent tag is the WOF parent ROW's placetype projected through PLACETYPE_PROJECTION, so a locality/localadmin parent reads `locality` and a borough parent reads `dependent_locality`. This artifact deliberately ships WITHOUT a `parentDelta`: no board has graded the parent side for this locale, and the D-rule's answer to an unmeasured locale is a per-locale check rather than a magnitude inherited from us/gb/nz/fr. Absent means the parent bias is OFF here, not zero.",
			"delta_calibration": "δ=10, transitionBeta=5 — inherited from the GB calibration per R5's finding that the classifier's dependent_locality deficit is uniform across locales.",
			"country_check": "hard-blocked on the header's country (de).",
			"requires_leading_postcode_strip": "This artifact is INERT without the `de` entries in SEGMENT_PARENT_POSTCODE_SHAPES and LEADING_POSTCODE_COUNTRIES (neural/placetype-pair-prior.ts). German addresses write '50733 Köln', so a parent segment folds to a key no bare-Gemeinde entry matches — measured: the index alone changed nothing until both entries landed. Same dependency FR's artifact carries.",
			"not_committed": "derived artifact, .gitignore'd — rebuilt via `mailwoman gazetteer pair-index --out neural-weights-de-de --country de --delta 10 --transition-beta 5 --pairs-jsonl <de-pairs.jsonl>`",
			"$comment_currency": "CURRENCY FILTER (2026-08-02): both ends of every pair are now required LIVE — `is_current != 0 AND is_deprecated = 0`. The WOF ingest hard-rejects `wof:superseded_by` records but RECORDS `edtf:deprecated` and `mz:is_current` as columns rather than rejecting them, so filtering is the reader's job and this extraction was not doing it. Pair count 85605 → 85603."
		}
	},
	"eval": {
		"r9_de_ortsteil": {
			"venue_confound_board": {
				"n": 70,
				"false_positives": 0,
				"fp_pct": 0.0,
				"bar_pct": 2.0,
				"note": "45 directional-class surfaces (Nord/Süd/Ober/Neu…, from 9,406 available) + 25 others, each opening a German venue name"
			},
			"positive_board": {
				"n": 60,
				"emit": 60,
				"tag_correct": 60,
				"tag_correct_pct": 100.0,
				"bar_pct": 70.0
			},
			"fusion_fix": "Without the index 'Venloer Str. 300, Ehrenfeld, 50823 Köln' fused the locality as 'Ehrenfeld Köln'; with it the two split correctly."
		}
	},
	"files": {
		"model_card": "model-card.json",
		"pair_index": "pair-index-de.bin"
	}
}
