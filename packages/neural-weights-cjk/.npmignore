# Present so `npm publish` honors package.json's `files` whitelist instead of
# falling back to .gitignore (which excludes model.onnx — gitignored on purpose,
# but it MUST be packaged at publish time).
