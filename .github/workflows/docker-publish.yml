# Builds and pushes the ghcr.io/sister-software/mailwoman container image.
#
# The Dockerfile installs the published @mailwoman/* packages from npm instead of building the
# monorepo, so this build also tests the published packages. The image bakes in the model weights.
# The gazetteer is mounted as a volume at /data.
#
# A manual run takes an explicit `tag` input. A published GitHub release tags the image with the
# release version. The `latest` tag is skipped for prereleases.
#
# The image is linux/amd64 only because onnxruntime-node ships glibc x64 prebuilds.
# The job authenticates with the runtime GITHUB_TOKEN, which needs packages:write.

name: Docker publish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Image version tag (e.g. 7.2.1). Also pushed as :latest."
        required: true
        type: string
  release:
    types: [published]

permissions:
  contents: read
  packages: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  IMAGE_NAME: ghcr.io/${{ github.repository }}

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-push:
    runs-on:
      - self-hosted
      - Linux
      - X64
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # The dispatch input wins. Otherwise the release tag is used without its leading "v".
      - name: Resolve version
        id: version
        env:
          DISPATCH_TAG: ${{ inputs.tag }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          if [ -n "$DISPATCH_TAG" ]; then
            value="$DISPATCH_TAG"
          else
            value="${RELEASE_TAG#v}"
          fi
          if [ -z "$value" ]; then
            echo "::error::could not resolve a version tag"
            exit 1
          fi
          echo "value=$value" >> "$GITHUB_OUTPUT"
          echo "Resolved image version: $value"

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker metadata (labels)
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ steps.version.outputs.value }}
            type=raw,value=latest,enable=${{ github.event_name == 'workflow_dispatch' || !github.event.release.prerelease }}
          labels: |
            org.opencontainers.image.title=mailwoman
            org.opencontainers.image.source=https://github.com/sister-software/mailwoman
            org.opencontainers.image.url=https://mailwoman.ai
            org.opencontainers.image.documentation=https://mailwoman.ai/docs
            org.opencontainers.image.licenses=AGPL-3.0-only OR LicenseRef-Commercial
            org.opencontainers.image.vendor=Sister Software
            org.opencontainers.image.version=${{ steps.version.outputs.value }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
