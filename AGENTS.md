# AGENTS.md

Mailwoman is a postal-address parser shipped as the unscoped entry package `mailwoman` (CLI + library) plus **69 scoped `@mailwoman/*` workspaces** — **70 entries in the root `workspaces` array**, which is the list to trust. The repo root is the private orchestration package `@mailwoman/universe` (not published).

**59 of the 70 publish to npm.** The publish set is `.release-it.json`'s `@release-it-plugins/workspaces` list, and it is not derivable from the `private` flag alone: ten workspaces are `private: true` and can never publish (`docs`, `tile-worker`, `license-worker`, `geocode-oracle`, `neural-weights-base-latn`, `dev-mcp`, `release-kit`, `repo-health`, `ops-cli`, `release-mcp`), and **`osm` is public but deliberately held OUT of the release list** — ODbL counsel sign-off pending (`packages/osm/README.md`). That is eleven, and 70 − 59 = **eleven**, so every absence currently has a reason someone can state. Trust the subtraction over the story: run the check in the release-list pitfall below whenever you add or move a workspace, and when the number stops matching the workspaces you can name a reason for, one has fallen out of the release. The table groups them by role:

| Workspace                                                                    | npm package                                     | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/mailwoman/`                                                        | `mailwoman`                                     | CLI + the high-level runtime pipeline (`createRuntimePipeline`) — the user-facing entry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Runtime pipeline (staged preprocessing → parse → resolve)**                |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/normalize/`                                                        | `@mailwoman/normalize`                          | Stage 1 — deterministic preprocessing (Unicode NFC, punctuation, whitespace, abbreviation); pure, no ML                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/query-shape/`                                                      | `@mailwoman/query-shape`                        | Cheap structural priors — character class, segmentation, known-format detection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/locale-hint/`                                                      | `@mailwoman/locale-hint`                        | Stage 2 — derive a `LocaleHint` from the query shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/kind-classifier/`                                                  | `@mailwoman/kind-classifier`                    | Stage 2.5 — categorize by query shape (`postcode_only` / `structured_address` / `po_box` / `intersection` / …)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/phrase-grouper/`                                                   | `@mailwoman/phrase-grouper`                     | Stage 2.7 — propose coherent input units (boundary discovery) with a kind hypothesis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Model + core**                                                             |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/sqlite/`                                                           | `@mailwoman/sqlite`                             | The one place a SQLite connection is constructed. `DatabaseClient` extends Kysely over `node:sqlite` and is `Disposable`, so `using db = new DatabaseClient<Schema>(path)` closes it at scope exit; `node:sqlite` is imported nowhere else.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/core/`                                                             | `@mailwoman/core`                               | Tokenization, decoder, policy registry. Ships ~9 MB libpostal + WOF + chromium-i18n data under `packages/core/data/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/api-kit/`                                                          | `@mailwoman/api-kit`                            | API plumbing: Hono node serve wrapper, OpenAPI emit helpers, generic timing metrics, native error envelope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/neural/`                                                           | `@mailwoman/neural`                             | SentencePiece tokenizer + ONNX inference + decoder wiring                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/sentencepiece-wasm/`                                               | `@mailwoman/sentencepiece-wasm`                 | google/sentencepiece v0.2.2 → WASM with the native-offsets binding — the tokenizer core (single-file ESM, committed artifact, rebuilt via `build.sh`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/neural-web/`                                                       | `@mailwoman/neural-web`                         | **DEPRECATED** — the browser runtime moved INTO `@mailwoman/neural`. This only re-exports it so existing imports keep working; migrate to `@mailwoman/neural/web-loader` + `/web-onnx-runner`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/neural-weights-en-us/`                                             | `@mailwoman/neural-weights-en-us`               | Trained model bundle (en-us) — data-only, loaded by `@mailwoman/neural`. The self-contained BASE: every overlay below shares its `model.onnx` + `tokenizer.model` byte-for-byte                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/neural-weights-fr-fr/`                                             | `@mailwoman/neural-weights-fr-fr`               | Data-only overlay (fr-fr) — ships only the fr-specific data siblings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/neural-weights-en-gb/`                                             | `@mailwoman/neural-weights-en-gb`               | Data-only overlay (en-gb). Ships `pair-index-gb.bin` + `postcode-gb.bin`; the postcode-anchor channel is ON, the GB anchor slot having been trained                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/neural-weights-en-au/`                                             | `@mailwoman/neural-weights-en-au`               | Data-only overlay (en-au) — country scope only. Ships NO postcode-au.bin, NO pair-index-au.bin, NO anchor-lexicon; exists so --locale en-AU constrains the resolver's country scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/neural-weights-en-nz/`                                             | `@mailwoman/neural-weights-en-nz`               | Data-only overlay (en-nz). Ships no postcode binary either — no WOF NZ postcode extract exists yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/neural-weights-de-de/`                                             | `@mailwoman/neural-weights-de-de`               | Data-only overlay (de-de) — ships the DE placetype-pair index plus the four evidence lexicons the base card requires                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/neural-weights-es-es/`                                             | `@mailwoman/neural-weights-es-es`               | Data-only overlay (es-es) — ships the ES placetype-pair index plus the four evidence lexicons the base card requires                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/neural-weights-it-it/`                                             | `@mailwoman/neural-weights-it-it`               | Data-only overlay (it-it) — ships the IT placetype-pair index plus the four evidence lexicons the base card requires                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/neural-weights-en-in/`                                             | `@mailwoman/neural-weights-en-in`               | Data-only overlay (en-in) — ships the IN placetype-pair index plus the four evidence lexicons the base card requires                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/corpus/`                                                           | `@mailwoman/corpus`                             | BIO-labeled training-corpus pipeline. One of the two workspaces that nests under `src/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Reference data**                                                           |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/codex/`                                                            | `@mailwoman/codex`                              | Per-address-system postal reference + branded types (USPS suffixes, US ZIPs). Pure, zero-runtime-dep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/variant-aliases/`                                                  | `@mailwoman/variant-aliases`                    | Regional variant alias table (`servo` → fuel en-AU, `PFK` → KFC fr-CA), switched on per locale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/poi-taxonomy/`                                                     | `@mailwoman/poi-taxonomy`                       | POI category taxonomy + phrase lexicon — Overture category snapshot, infrastructure extension namespace, synonym table (`drinking fountain` → `drinking_water`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/activity-lexicon/`                                                 | `@mailwoman/activity-lexicon`                   | Reviewed activity-phrase lexicon — the surface forms a person types for an activity (`pick up a prescription` → `obtain_medication`), each carrying the committed record that attests it and the locales it is used in. Zero dependencies; recognition only, and it states nothing about the world. Discipline: `packages/activity-lexicon/data/PROVENANCE.md`                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/geographic-model/`                                                 | `@mailwoman/geographic-model`                   | World semantics — concepts, relations, activities/affordances, mappings into external vocabularies, source observations, derived facts, provenance, deterministic compilation. NO ranking weights, boosts, penalties, or ordering APIs; `@mailwoman/core` must not depend on it. Boundary record: `docs/superpowers/specs/2026-08-26-geographic-model-boundaries.md`                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/evidence/`                                                         | `@mailwoman/evidence`                           | The typed-evidence contract — `observation` / `relation` / `prior` constructors and the `EpistemicStatus` axis (`designated` / `observed` / `derived` / `inferred` / `unresolved`), which says what may be CLAIMED about a value, separate from the mechanism that produced it. Zero runtime dependencies by design. An authoritative relation refuses a score, the rule `filer.db` enforces in SQL. Spec: `docs/superpowers/specs/2026-08-21-evidence-derivation-design.md`                                                                                                                                                                                                                                                                  |
| **Resolver + geo**                                                           |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/resolver/`                                                         | `@mailwoman/resolver`                           | Walk an `AddressTree`, decorate nodes with gazetteer coords + attribution; backend-agnostic, owns the span-rescore weights                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/resolver-wof-sqlite/`                                              | `@mailwoman/resolver-wof-sqlite`                | WOF SQLite resolver — FTS5/candidate-table `PlaceLookup` (the geocoding backend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `packages/resolver-wof-wasm/`                                                | `@mailwoman/resolver-wof-wasm`                  | WOF WASM resolver — browser `PlaceLookup` over `@sqlite.org/sqlite-wasm`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/spatial/`                                                          | `@mailwoman/spatial`                            | Spatial/geo utilities — haversine, bbox, projection (the math home)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/cartographer/`                                                     | `@mailwoman/cartographer`                       | Mapping utilities for the geo data pipeline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/tiger/`                                                            | `@mailwoman/tiger`                              | US Census TIGER/Line processing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/ban/`                                                              | `@mailwoman/ban`                                | BAN (Base Adresse Nationale) FR national address register — fetch + per-region extract provider (`packages/ban/lib/sdk`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/osm/`                                                              | `@mailwoman/osm`                                | OpenStreetMap rooftop address-point ingestion — fetch + per-region extract provider (`packages/osm/lib/sdk`); publish blocked on counsel sign-off                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **FCC broadband data (layer providers — off the parse path)**                |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/bdc/`                                                              | `@mailwoman/bdc`                                | FCC Broadband Data Collection availability — fetch/parse/ingest provider plus the `bdc.db` layer reader. Never ingests or derives from the CostQuest-licensed Fabric; `location_id` is carried as an opaque join key only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/filer/`                                                            | `@mailwoman/filer`                              | FCC filer identity crosswalk — builds/reads `filer.db` linking FRN / Form 499 filer ID / BDC `provider_id` / holding + management company. Provenanced and `asOf`-scoped; authoritative vs name-matched links stay separate, never conflated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Authority designations (layer providers — off the parse path)**            |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/flood/`                                                            | `@mailwoman/flood`                              | Environment Agency "Flood Map for Planning — Flood Zones" (England) — acquisition, the sealed `flood.db` two-tier polygon layer, and its reader. Zone 1 is represented by ABSENCE, so a designated absence and an unmapped location are separate readings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/soil/`                                                             | `@mailwoman/soil`                               | USDA NRCS SSURGO soil survey (Iowa pilot) — acquisition, the sealed `soil.db` polygon layer, and its reader. One per-cell artifact serves BOTH consumers: the area-weighted class DISTRIBUTION with four separate absence shares, never a winner class.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/coastal/`                                                          | `@mailwoman/coastal`                            | Environment Agency National Coastal Erosion Risk Mapping (NCERM National 2024, England) — acquisition, the sealed `coastal-england.db` polygon layer, and its reader. SCENARIO-SCOPED: twelve erosion-zone layers (management × horizon × climate allowance) that must never be pooled, and every reading names the one it answered under. Coverage is `source_present` and licenses NO negative claim — the INVERSION of flood's Zone-1-by-absence rule.                                                                                                                                                                                                                                                                                     |
| `packages/zoning/`                                                           | `@mailwoman/zoning`                             | Ireland's Generalised Zoning Types (MyPlan.ie, Department of Housing, Local Government and Heritage) — acquisition, the sealed `zoning-ireland.db` polygon layer, and its reader. VOCABULARY-FIRST: the local authority's own zone code is carried VERBATIM and the publisher's national generic type sits beside it, never instead of it — 52 of 795 (authority, code) pairs take more than one generic type, so `zoning_crosswalk_edge` ships empty and the mapping stays per polygon. Hole roles come from ring ORIENTATION (clockwise is exterior, the inverse of RFC 7946). Coverage is `source_present` and licenses NO negative claim. The DATA is `tier: build-local`: three published statements disagree about the source's license |
| **Formatter + record matcher (geocode-first entity resolution)**             |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/formatter/`                                                        | `@mailwoman/formatter`                          | The inverse of the parser — render `ComponentTag` components to an address string + a canonical match key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/record/`                                                           | `@mailwoman/record`                             | Record schema + per-field normalizers for the matcher                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/match/`                                                            | `@mailwoman/match`                              | The geocode-first matcher — block → score → cluster (string comparators + the Fellegi-Sunter scorer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/registry/`                                                         | `@mailwoman/registry`                           | The record-matching app — resolve messy records to geocoded entities, export GeoJSON                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/address-id/`                                                       | `@mailwoman/address-id`                         | Stable address primary key (`<state>.<H3-cell>.<hash>`) — the exact-match complement to the fuzzy matcher                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Drop-in APIs + annotations (replace Nominatim/Photon/libpostal/OpenCage)** |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/annotations/`                                                      | `@mailwoman/annotations`                        | The annotation contract — native `AnnotationSet` + `toOpenCage()`/`toNative()`; the interface every annotator plugs into                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/api/`                                                              | `@mailwoman/api`                                | The native HTTP API — engine-agnostic `/v1` surface (parse, geocode, batch, resolve, format) with health/metrics + emitted OpenAPI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/nominatim/`                                                        | `@mailwoman/nominatim`                          | Nominatim-compatible drop-in (`/search`, `/reverse`, `/status`) — returns the OpenCage-style `annotations` block                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `packages/photon/`                                                           | `@mailwoman/photon`                             | Photon-compatible autocomplete drop-in (`/api`, `/reverse`) — GeoJSON FeatureCollections                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/libpostal/`                                                        | `@mailwoman/libpostal`                          | libpostal-compatible drop-in (`/parse`, `/expand`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/timezone-lookup/`                                                  | `@mailwoman/timezone-lookup`                    | Server-side IANA timezone PIP lookup (node:sqlite); data built like the gazetteer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `packages/un-locode-lookup/`                                                 | `@mailwoman/un-locode-lookup`                   | UN/LOCODE nearest-code lookup (node:sqlite)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/nuts-lookup/`                                                      | `@mailwoman/nuts-lookup`                        | EU NUTS region PIP lookup (node:sqlite)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/fastify/`                                                          | `@mailwoman/fastify`                            | Fastify plugin — mounts the local pipeline as geocode/parse/POI routes plus a `fastify.mailwoman` decorator. `0.0.1` is a NAME RESERVATION; the first functional release ships with the next mailwoman minor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/mcp/`                                                              | `@mailwoman/mcp`                                | MCP server — mailwoman's spatial toolset for agents over stdio (parse, geocode, poi_search, overpass_export, layer_manifest)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Client + UI**                                                              |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/react/`                                                            | `@mailwoman/react`                              | React components + headless hooks — the docs site's parse/geocode/POI explorers, decomposed for use in any React app. Storybook-tested, Docusaurus-independent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/map-tui/`                                                          | `@mailwoman/map-tui`                            | Terminal vector-tile map renderer — braille `MapFrame` values from PMTiles (asciify-based); powers the `mw geocode --debug` map pane, plus the `map-tui` bin — a standalone browser over those frames. Frame-first: consumers own presentation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Private / infra**                                                          |                                                 |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `docs/`                                                                      | `@mailwoman/docs` (private)                     | Docusaurus site → https://mailwoman.ai. The other workspace that nests under `src/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/tile-worker/`                                                      | `@mailwoman/tile-worker` (private)              | Tile worker for the demo-map coverage overlay                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/license-worker/`                                                   | `@mailwoman/license-worker` (private)           | The self-service license worker on Cloudflare: Stripe webhooks in, signed license tokens out, ledger in D1; the claim, refresh and status routes. Two Wrangler environments; deploys by manual dispatch; imports four `@mailwoman/core` subpaths and never the barrel. Its `./shop` subpath is the shop as data (`lib/shop/catalog.ts`) plus `mwops shop status                                                                                                                                                                                                                                                                                                                                                                               | provision`, which reconciles a Stripe account against it Spec: `docs/superpowers/specs/2026-09-05-self-service-commercial-license-design.md` |
| `packages/geocode-oracle/`                                                   | `@mailwoman/geocode-oracle` (private)           | Third-party reference geocoders (Google, US Census) as a VERIFICATION ORACLE for gauntlet-case authoring. Both clients answer with `@mailwoman/record`'s `PostalAddress` so the comparison is field-to-field. Never a runtime dependency of the parser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `packages/dev-mcp/`                                                          | `@mailwoman/dev-mcp` (private)                  | Maintainer-only MCP server — a warm geocoder plus measuring tools, so an agent reaches for the board rather than a one-off probe script. Holds engines in-process and REFUSES on a source edit rather than serving stale modules. Spec: `docs/superpowers/specs/2026-08-15-dev-mcp-server.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/release-kit/`                                                      | `@mailwoman/release-kit` (private)              | Release operations as a REGISTRY (`lib/registry.ts` is the only executable entry point): preflight, prepare, pack, verify, stage, publish, weights materialization, each with an `effect` (`read` / `local-write` / `external-write`), zod schemas and `run`. Consumed through adapters only. Publishing is plan → execute on a digest. Record: `docs/superpowers/specs/2026-09-04-scripts-directory-migration-proposal.md`                                                                                                                                                                                                                                                                                                                   |
| `packages/repo-health/`                                                      | `@mailwoman/repo-health` (private)              | Repository health checks as a REGISTRY: each `RepoCheck` inspects the checkout and returns diagnostics; nothing here mutates, generates, publishes, benchmarks or probes. `mwops health all` runs it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/ops-cli/`                                                          | `@mailwoman/ops-cli` (private)                  | `mwops`, the private operator CLI — a view over the three registries (release-kit, repo-health, the license worker's shop) for CI and humans, with no logic of its own. Workflows call `mwops …`, never a `lib/*.ts` path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/release-mcp/`                                                      | `@mailwoman/release-mcp` (private)              | Maintainer-only MCP server over `@mailwoman/release-kit`'s registry: one tool per operation, the declared `effect` in every description, `dry_run` on the writers. The two `external-write` operations are off the tool list unless the server starts with `--allow-external-write`, and even then run the plan → execute contract. No release logic of its own                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/ancestrie/`                                                        | `@mailwoman/ancestrie`                          | A materialized trie over an ancestry graph — one prefix walk yields completions, ranks, and containment lineage from one static artifact. Standalone by design (#1728); lineage + format doc in the package                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/neural-weights-base-latn/`                                         | `@mailwoman/neural-weights-base-latn` (private) | PARKED, not published — the shared Latin-script base model for the consumer-facing dedup (#1177). Do NOT add it to `release.config.json` locales or depend on it from a published package until the release wiring lands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

Source files live at each workspace's root, with exactly two exceptions: **`packages/corpus/` and `docs/` nest under `src/`**. Every workspace lives under `packages/` except `docs/`, which stays at the repository root.

Beyond the 64 workspaces the repo root holds workspace config and six directories that are **not** workspaces — do not go looking for them in the table:

- `apps/web-demo/` — a standalone Python + single-page-HTML parser demo (`server.py`, no `package.json`), served behind the playpen nginx
- `corpus-python/` — the Python training code, including the Modal launcher
- `hf-publish/` — three Hugging Face model/dataset card READMEs, nothing else
- `data/` — checked-in reference inputs (e.g. `data/gazetteer/anchor-lexicon-v1.json`, the copy `mailwoman eval promote` runs against)
- `evals/` — the per-model score ledger (`evals/scores-by-version.json`)
- `docker/` — the private `mailwoman-docker` dependency manifest for the ghcr image. It HAS a `package.json`, which makes it look like a workspace; it is not in the `workspaces` array, and it installs the PUBLISHED `@mailwoman/*` packages rather than the local ones

## Where to read next

- **[`docs/records/site-2026-08/concepts/what-mailwoman-is.mdx`](./docs/records/site-2026-08/concepts/what-mailwoman-is.mdx)** — what the system _is_ (a calibrated, retrieval-augmented sequence labeler), the grammar/atlas division of labor, and the disciplines that keep the architecture accurate. Read this first if you're new to the project entirely.
- **[`docs/engineering/SCOPE.mdx`](./docs/engineering/SCOPE.mdx)** — the current scope declaration (#886): locale tiers, the two workstreams, the standing invariants, the runtime-flag register, where the live roadmap lives. Read this first for orientation. The old implementation plan (`plan/README.mdx`) and the phase directory are historical design records — rationale, not current state.
- **[`docs/engineering/reference/`](./docs/engineering/reference/)** — design rationale, the schema definition, TS contracts, operations playbook, address-data-sources catalog. `SCHEMA.mdx` is the single canonical definition for the `ComponentTag` union.
- **[`docs/engineering/CONTRIBUTING_MODEL_WORK.mdx`](./docs/engineering/CONTRIBUTING_MODEL_WORK.mdx)** — the model-work runbook: which evals must pass before a change ships, the change-shape taxonomy ("Which tool for which tag"), how to add a corpus recipe. Read before touching the model, a corpus recipe, or an eval. Iron rule 6 is the **D-rule**: no default-on mechanism (model OR decode-time) ships with a known regression vs the shipped model on any tier-1 locale — fix it, limit it to one locale, or make it opt-in.
- **[`docs/engineering/reference/decoder-grammar.mdx`](./docs/engineering/reference/decoder-grammar.mdx)** — the decoder grammar contract: the objective the shipped decoder maximizes (token-factorized, structural transitions only), the target span-factorized objective, why token-additive registry evidence subsidizes split parses, the toponymy vocabulary (specific/generic/modifier, `G_name`), and the constraint taxonomy as measurable predicates with the rule that no constraint becomes a decode-time term before its detector's census is read. Read before changing the decoder, a prior, or proposing a phrase-cohesion fix.
- **[`docs/engineering/reference/layer-contract.mdx`](./docs/engineering/reference/layer-contract.mdx)** — the spatial-layer contract for layer authors: the `layer_manifest`/`layer_coverage` schema every layer database embeds, the H3/WOF/address-id spine, the shipped/build-local/private tiers, the meaning-of-zero coverage rule, and the `@mailwoman/core/layers` API. Read before authoring a new layer (poi.db is layer #1 — see the runbook below for the worked example).
- **[`docs/engineering/reference/poi-layer-runbook.mdx`](./docs/engineering/reference/poi-layer-runbook.mdx)** — the poi.db build/verify/publish runbook: the Overture Places schema gotchas (no top-level `country`; the `taxonomy`-vs-`categories` transition), the res-9 clustering decision, the fixtures→smoke→full-scale verification ladder, ODbL build-local posture, the demo publish decision. Read before touching `gazetteer build poi`, the POI executor, or the layer publish path.
- **[`docs/engineering/reference/coverage-overlay.mdx`](./docs/engineering/reference/coverage-overlay.mdx)** — the demo-map address-coverage ("fog of war") overlay runbook: rebuild/republish (the `coverage build` + `tiles publish` CLIs), the `nexus-assets` upload/credential gotchas, the HI/NH data situation, tuning knobs. Read before touching the coverage overlay, the `tile-worker`, or `coverage build`.
- **[`RELEASING.md`](./RELEASING.md)** — local and CI release flows. Read before publishing a release.
- **[`docs/records/evals/`](./docs/records/evals/)** — eval reports per training step. The per-model score ledger lives at `evals/scores-by-version.json` (repo root), schema in `docs/engineering/reference/eval-ledger.schema.json`. Appending is automated: `mailwoman eval ledger-append`, and `mailwoman eval promote` prints the pre-filled command on every PASS. Versions with no ledger row have their headlines in [`docs/records/site-2026-08/releases.mdx`](./docs/records/site-2026-08/releases.mdx) and each version's `model-card.json`. The authoritative human-readable per-tag table is the latest `parity-scorecard-*.md`.
- **[`.claude/output-styles/mailwoman-development.md`](./.claude/output-styles/mailwoman-development.md)** — the central style contract for agent output. The harness loads it as the session output style; the `five-whys` skill, PR template, and issue templates link to it instead of copying its rules. It covers commands, runbooks, handoffs, warnings, evidence, and root-cause reports. Its executable half is `docs/.vale-chat.ini` (the shared `Mailwoman` Vale style plus the chat-only `MailwomanChat` rules), run over every finished reply by the Stop hook `packages/dev-mcp/lib/hooks/vale-response-check.ts` (Codex: the same check via `.codex/hooks.json` → `vale-response-check-codex.ts`; both adapters share `vale-check-core.ts`) and fixture-tested by `docs/scripts/check-vale-rules.ts`. Published docs pages follow [`writing-system.md`](./docs/engineering/writing-system.md) instead.

### Four words are banned from prose

`docs/styles/Mailwoman/AmbiguousShorthand.yml` refuses four nouns that each stand for four or five different things here, so a reader cannot tell which one a sentence means. This file does not list them, so that the words never enter an agent's context through the instructions; read the rule file when a finding names one. Because the rule lives in the shared `Mailwoman` style it applies to published pages, this file, every package README, the engineering docs — and to agent replies through `.vale-chat.ini`. Name the concrete thing instead: the check by its name (the promotion eval, the verify step, the D-rule, the required `test` CI context) or a plain verb (blocks, requires, refuses); the artifact by its filename; the boundary by its interface; and for a release, publish it.

Contract-bearing names keep their spelling. `@mailwoman/locale-hint`, `mailwoman eval promote`, `mwdev_promotion_eval`, `promotion-eval.ts`, `packages/corpus/lib/recipes/` and `RegionDatabaseProvider` are unchanged — Vale skips inline code, so backticking an identifier in running prose is both the remedy and correct markdown. Renaming one of them is a separate, operator-approved change (#1937) — which is what happened to the family of names for a corpus slice, a WOF extract and a region database: that word is now removed outright, at **zero occurrences**, enforced by `repo-health`'s `bannedVocabulary` counter. The renames are recorded in the #1937 receipt. Do not reintroduce it, in prose or in comments; the four concepts it used to cover are a corpus recipe, a corpus slice, a WOF extract, and a region database. Three configs run the rule: `.vale.ini` over the published corpus, `.vale-vocab.ini` over the root markdown + `docs/engineering` + package READMEs (`yarn lint:prose`, part of `yarn lint`), and `.vale-chat.ini` over replies. Dated point-in-time records are out of scope, the same exemption the acronym-casing convention gives them.

## Release pipeline pitfalls

Local: `yarn release`. CI: GitHub Actions → `publish` workflow → manual dispatch. See `RELEASING.md` for setup + flow. The notes below cover the gotchas that have bitten this pipeline before — read before touching `packages/release-kit/lib/weights/copy-weights.ts`, `packages/release-kit/lib/pack/publish-workspace.ts`, or `.release-it.json`. Every release step CI runs is `yarn mwops release <operation>` over `@mailwoman/release-kit`'s registry (`packages/release-kit/lib/registry.ts`); `yarn mwops` lists the operations with their declared effect, and the two `external-write` operations (`publish-workspace`, `bless-package`) take a `--plan <file>` written by `mwops release plan --json` and refuse a dirty or moved HEAD or a changed digest. release-it's per-workspace `publishCommand` hook cannot supply a plan, so it passes `--allow-unplanned`, which logs the unverified publish.

### The weights workspaces have moving binaries

The `neural-weights-<locale>` workspaces ship binary artifacts (`model.onnx`, `tokenizer.model`) that are **not** committed to git. Multiple pieces cooperate to get those files in place — be careful when changing any of them:

- `<workspace>/scripts/link-dev-weights.ts` — materializes the artifacts from `$MAILWOMAN_DATA_ROOT/...` into the **data-root overlay** (`$MAILWOMAN_DATA_ROOT/weights/<locale>/`), NOT into the tracked workspace — the workspace package stays bare on a dev checkout (writing into it caused the four hazards `packages/release-kit/lib/weights/link-weights-overlay.ts`'s header lists: empty worktrees, `yarn test` mutating tracked dirs, copy-through-symlink, `YN0035` tarballs). `resolveWeights`'s overlay rung is what reads it in local dev; a test asserting the consumer's package-carries-weights state must seed its own scratch overlay instead (`dropin-cold-start.test.ts`, #1733).
- `packages/neural/test/weights.test.ts` — invokes `link-dev-weights.ts` to verify auto-resolve. **Running `yarn test` re-populates the data-root overlay** as a side effect.
- `mwops release copy-weights` (`packages/release-kit/lib/weights/copy-weights.ts`) — invoked by release-it's `before:init` hook. Materializes the real binaries into each workspace. Skipped in CI when `MAILWOMAN_SKIP_WEIGHTS_COPY=1` (the default for the `publish` workflow when `release_weights=false`).
- `mwops release publish-workspace` (`packages/release-kit/lib/pack/publish-workspace.ts`) — invoked per workspace by release-it's hook (with `--allow-unplanned`) and by `publish.yml`'s loop (with `--plan`). Calls `yarn pack -o <tmp>` (translates `workspace:*` → concrete versions) then `npm publish <tmp>` (npm CLI handles npm-side auth, including Trusted Publishing OIDC in CI).

### Pitfall: the overlay `link-dev-weights.ts` is a copy-paste template

Each locale overlay carries its own `scripts/link-dev-weights.ts`, cloned from a sibling. Copying localizes the CODE and leaves the PROSE behind, and nothing catches a docstring that describes the wrong country — the script works. If you add an overlay, rewrite the docstring for the locale you are adding, not the paths alone. The shared implementation is `materializeDevOverlay` in `@mailwoman/resolver-wof-sqlite/weights-overlay-linker`, so each script is a manifest plus a call (#2035); the one step no manifest expresses, en-gb's card-conditional postcode binary, stays in en-gb's file. Membership in `LEADING_POSTCODE_COUNTRIES` (`packages/neural/lib/placetype-pair-prior.ts`) is earned by a codex postcode shape plus a confound board; a country's absence there is not always a gap, and the file says which absences are deliberate.

### Pitfall: symlinks in the publish tarball

`yarn npm publish` (and `npm publish`) refuse to upload tarballs containing symlinks — the registry returns HTTP 415 (`YN0035: Symbolic link is not allowed`). Two specific traps make this easy to hit:

1. **`fs.copyFile` follows symlinks at the destination.** A naïve `fs.copyFile(SOURCE, dest)` where `dest` is a symlink writes through the symlink — the symlink stays in place. `copy-weights.ts` mitigates this by `unlink`ing each destination first. Any new script that materializes files into these workspaces **must do the same** (or use `cp --remove-destination` / `fs.cp` with equivalent semantics).
2. **Tests re-created symlinks — historically.** `weights.test.ts` calls `link-dev-weights.ts` on every run; while the linkers wrote into the tracked workspaces, any `yarn test` after `copy-weights.ts` re-symlinked `packages/neural-weights-en-us/`. The linkers now write to the data-root overlay instead, which closes that window by construction — but the safety net below stays, because it also covers a hand-made symlink or an old checkout.

To make publish work regardless of repo state, `mwops release publish-workspace` walks the workspace's `package.json` `files` array right before publishing and dereferences any symlinks (`readlink` → `unlink` → `copyFile`). **Do not remove this safety net** — it closes the window between `copy-weights.ts` (one-shot, at `before:init`) and the actual publish.

### Pitfall: provenance attestation needs the CI identity, not a public repo alone

`mwops release publish-workspace` adds `--provenance` to `npm publish` when `GITHUB_ACTIONS` is set and `MAILWOMAN_NPM_PROVENANCE` is not `0`. Two things decide it and they are easy to conflate. The repo must be PUBLIC, because a sigstore attestation links to source third parties have to be able to verify — that half is satisfied. The publish must also run on a CI provider npm supports, because the attestation is signed against an OIDC identity: a local `yarn release` passing `--provenance` fails outright, which is why the predicate is `GITHUB_ACTIONS` and not the generic `CI`. Trusted Publishing works either way; only the attestation needs the CI identity. Set `MAILWOMAN_NPM_PROVENANCE=0` to publish without one when sigstore or the registry is down.

### Pitfall: `workspace:*` doesn't survive `npm publish`

`yarn 4`'s `workspace:*` protocol is yarn-specific. `npm publish` ships the literal string and consumers hit `EUNSUPPORTEDPROTOCOL`. `yarn pack` translates `workspace:*` to the concrete sibling version in the tarball. That's why `publish-workspace.ts` does pack-then-publish instead of either tool alone.

### Pitfall: `exports` carries a dev-only `node → .ts` condition — `publishConfig.exports` is INJECTED at pack time, never committed

Every workspace's committed `exports` map is the DEV map only: curated subpaths with a `node` condition first pointing at `.ts` source (plain `node` runs source in the repo). Consumers can never use that condition — Node refuses to type-strip under `node_modules` (`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`) — so `packages/release-kit/lib/pack/pack-workspace.ts` derives `publishConfig.exports` from the dev map at pack time (strip `node → .ts`, `types` first), lets `yarn pack`'s publishConfig substitution write it into the tarball manifest, and restores the workspace file. A guard then refuses to publish if any exports target is missing from the tarball. Do NOT commit a `publishConfig.exports`; maintain ONLY the dev map. Tarballs ship source `.ts` + `out/` JS + `.d.ts` (the `files` arrays include all three).

### Pitfall: a workspace missing from the release list freezes — silently, and at consumer expense

`.release-it.json`'s workspaces list is the publish set AND the bump set. A workspace outside it is not merely unpublished: release-it never rewrites its `version` either, so it holds whatever number it had when it dropped out — in the repo and on npm — while every sibling moves on. Nothing fails; `yarn release` reports success because from its side there was nothing to do. The latent hazard is ordering: `yarn pack` freezes `workspace:*` to whatever the sibling reads AT PACK TIME, so a package packed before a release bump pins a base the overlay is behind.

A `private: true` flag is checkable; membership in a JSON array is not, so check the arithmetic whenever you add or move a workspace:

```bash
node -e "const w=require('./package.json').workspaces,r=require('./.release-it.json').plugins['@release-it-plugins/workspaces'].workspaces;console.log(w.filter(x=>!r.includes(x)))"
```

Every name it prints needs a reason you can state. `mwops release scaffold-weights-overlay` registers a new overlay in both lists for exactly this reason — a locale added by hand skips that step.

A new workspace joins SEVEN registers, and only the first one fails loudly. The root `workspaces` array is what `yarn install` reads, so missing it breaks immediately. The others do not: the release list above, or — when the workspace is held out — `SANCTIONED_RELEASE_ABSENCES` in `packages/release-kit/lib/release/stage.ts`, which is where "a reason you can state" stops being prose and starts being data (`checkReleaseListIdentity` refuses an absence that is in neither, and its `publishCount` pin in `packages/release-kit/test/integration/release-stage.test.ts` moves with either edit); BOTH root `tsconfig.json` reference entries — `./packages/<name>` and `./packages/<name>/tsconfig.test.json`; `mwops release smoke-clean-install`'s pack set (`packages/release-kit/lib/release/smoke-clean-install.ts`), which silently skips a workspace it does not name; and, for a brand-new npm name, the `bless-package` first publish before the name may enter the release list at all (an unblessed name fails the whole release at that workspace with a bare `E404`). Skip the tsconfig pair and `tsc -b` never builds the workspace, so `out/` stays empty and every test-project reference to it fails with `TS6305: Output file has not been built from source file` — which reads as a broken test rather than an unregistered project.

### Recovering from a partial release

The runbook is in [`RELEASING.md`](./RELEASING.md#recovering-from-a-partial-release).

## Workspace + test conventions

- **Source runs directly under `node`** (type stripping, no flags): relative imports use explicit `.ts` extensions, and each workspace tsconfig sets `rewriteRelativeImportExtensions: true` so `tsc` emits `.js` specifiers into `out/`. `erasableSyntaxOnly: true` is enforced everywhere — no `enum` (use `const X = {…} as const` + `type X = (typeof X)[keyof typeof X]`), no constructor parameter properties, no runtime namespaces. tsc rewrites dynamic `import()` string literals too, comments (`/* webpackIgnore: true */`) included.
- Each workspace has its own `tsconfig.json` + (for `packages/core/` and `packages/neural/`) a `vitest.config.ts` that aliases sibling `@mailwoman/*` subpath imports to source. This lets `yarn test` run without a precompile step. The cross-package aliases are fiddly — see the file headers for the resolution rules.
- **Source lives under `lib/`; tests live under `test/`.** Every `packages/*` workspace puts its TypeScript in `lib/`, and each `tsconfig.json` sets `"rootDir": "./lib"` with `"include": ["./lib/**/*"]`. `rootDir` STRIPS that segment from the emit, so `lib/utils/index.ts` compiles to `out/utils/index.js` and the published layout is unchanged — which is why only the dev-only `node` condition in `imports`/`exports` names `./lib/…` while `default`/`types` still name `./out/…`. Two consequences worth knowing before you edit build config: a workspace's `tsconfig.test.json` re-widens `rootDir` to `${configDir}` (via `tsconfig.test-base.json`) because `test/` sits outside `lib/` and TS6059 fires even under `noEmit`; and the ten data-only `neural-weights-*` packages plus `sentencepiece-wasm` have no `lib/` at all, because they have no `tsconfig.json` and compile nothing. `docs/` keeps Docusaurus's `src/`.
- `packages/core/lib/utils/repo.ts` computes the repo root and the `@mailwoman/core` package root by counting `..` up from `import.meta.url`. It carries NO source-vs-compiled branch, and that is deliberate rather than incidental: `lib/` and `out/` are siblings, so this file sits at the same depth in both trees and one constant serves both. It used to carry an `__isCompiledTree` flag, and that flag was wrong in production once (#481 — it overshot `out/` and sent dictionary reads to a nonexistent `core/out/data`). If a layout change ever breaks the equal-depth property, restore the equality rather than reintroducing the flag.
- **`sdk/` submodules mean data acquisition** (fetch/extract plus a `RegionDatabaseProvider` implementation — see `packages/ban/lib/sdk`, `packages/osm/lib/sdk`, `packages/tiger/lib/sdk`). CLI helpers live in `packages/mailwoman/lib/cli-kit/` (`useCommandTask`, `CheckList`, the command component types), the parser test harness in `packages/mailwoman/lib/test-kit/` — do not grow a new `sdk/cli`. **`@mailwoman/spatial` has no `sdk/`**: it held a pure WKT/WKB codec (now `spatial/lib/well-known-text.ts`, exported as `./well-known-text`) and an `ogrinfo` shell-out (now `spatial/lib/tools/ogr.ts`, exported as `./tools/ogr`), and acquired nothing. Those `./sdk*` subpaths were REMOVED rather than shimmed — see CHANGELOG "Unreleased".
- **This bullet has been written twice, so the third time is an edge check rather than a sentence.** The 2026-07-09 regroup moved `mailwoman/sdk/{cli,test}` out and declared the meaning restored; by 2026-09-01 `filer/lib/sdk/` and `bdc/lib/sdk/` held domain analysis again. A directory name cannot enforce itself, so `dependency-cruiser.config.cjs`'s `no-serve-package-to-build-tooling` enforces the part that matters: the five browser and request-path packages (`react`, `neural-web`, `tile-worker`, `api`, `fastify`) may not reach `lib/tools/` or `lib/sdk/` in ANY workspace. It is scoped by PACKAGE, not by folder name, because **`tools/` carries at least four senses here** — build tooling (`core`, `codex`, `corpus`), MCP tool definitions (`dev-mcp`), a CLI command's library half (`filer`, `mailwoman`), and product analysis (`registry`). The folder-name version of this rule was written first and produced 38 violations, every one of them correct behaviour.
- **HTTP clients extend or instantiate `APIClient`** (`@mailwoman/core/api`), not raw `fetch`. It carries what every fetcher otherwise re-derives: strict minimum-interval pacing (`minRequestIntervalMs`) or a `requestsPerMinute` budget, both applied downstream of the cache so a hit costs nothing; bounded retry honouring `Retry-After` (**off unless you set `retry`**); response caching (`@mailwoman/core/api/disk-storage` for an on-disk one); and error mapping to `ResourceError`, so callers branch on `error.status` / `isTransientResourceError(error)` rather than message prose. Inject `clock` and drive it with `@mailwoman/core/api/test-clocks` so timing tests never sleep. `packages/filer/lib/sdk/sec-client.ts` is the worked example; `packages/bdc/lib/sdk/client.ts` is the second, and covers what SEC didn't — a binary path (`responseType: "arraybuffer"` plus a per-request `cache: false`, which is how you turn caching off without touching `packages/core/lib/api`) and a per-minute budget. **`requestsPerMinute` does not deliver N requests per minute**: its cooldown is `60000/N` minus the gap since the last dispatch, so N alone dispatches N requests every `60/N` seconds — measured at 100/min for `requestsPerMinute: 10`. Set `minRequestIntervalMs` too (`60000/N`); that is the setting that holds the rate. **This rule binds API REQUESTS.** **API requests** — small bodies, repeated calls, rate-limited hosts — are the sites `APIClient`'s pacing, bounded retry, response caching and `ResourceError` mapping exist for; `packages/release-kit/lib/release/check-parity.ts` is the worked example of the migration, verified to produce a byte-identical verdict. **File transfers** are not: `packages/osm/lib/sdk/fetch.ts` and `packages/tiger/lib/sdk/download.ts` stream multi-gigabyte archives straight to disk, where response caching is nonsense, there is nothing to pace, and axios buffers any non-stream response type in memory. Those keep raw `fetch` and say so in place. Do not "finish the job" on them.
- **Filesystem work goes through `@mailwoman/core/fs`.** It is the one place in the tree that reaches `node:fs`; `oxlint.config.ts` refuses every `node:*` import outside `@mailwoman/core` and names the core home for each builtin in its message. Four modules: **`/readers` + `/writers`** answer promises — every reader and writer does, because no executable repository code makes a blocking filesystem call; **`/streams`** holds `openReadStream`/`openWriteStream`, which are neither, because a stream returns immediately and the work happens as the consumer pulls; **`/temporary`** owns a scratch directory (see the `mkdtemp` pitfall below). Every helper takes a `PathBuilderLike`, and the NAME carries the contract wherever two helpers differ only in what they forgive: `statPath` raises ENOENT where `tryStat` answers null, `removePath` raises where `removePathIfPresent` forgives, `makeDirectories` is recursive and idempotent where `makeDirectoryExclusive` raises EEXIST — and bare `mkdir` being an atomic test-and-set is how both of this repository's inter-process locks are held, so swapping one for the other would let every waiter take the lock at once. `pathExists` is the one deliberate NARROWING of a builtin: `existsSync` answers `false` for a path it could not read, and EACCES on a parent directory is not the same answer as "there is nothing here". Two ceremonies are encoded rather than repeated: every file writer creates the parent directory, and `copyFileTo`/`createSymbolicLink` clear the destination first, because `copyFile` onto a symlink writes THROUGH it (the defect that put symlinks in a publish tarball and made npm answer HTTP 415). File-DESCRIPTOR work — a partial read at an offset, a `FileHandle` held across calls — goes through `readFileRange` and `open` from `/readers`, so no file reaches past the idiom for it. `packages/repo-health/lib/checks/debt.ts`'s `synchronousFilesystemCalls` baseline is ZERO: the counter walks every tracked `.ts`/`.tsx` minus `packages/core/lib/fs/` for a builtin sync call or an import of a sync helper module, and one anywhere fails the check. One site reaches `node:fs/promises` for a reason the rule cannot see: `@mailwoman/neural`'s `tokenizer.ts` loads `readFile` through a dynamic `import(/* webpackIgnore: true */ "node:fs/promises")` so the browser bundle never meets it, and flattening that to a static `@mailwoman/core/fs/readers` pulls `#objects` and the rest of core into the client graph — which `browser-slo.test.ts` catches as an esbuild failure to resolve `fs`, and nothing else catches at all. `mailwoman/no-sync-fs-in-async` catches the regression that started all this: a synchronous call inside a function that is already `async`, which is a blocked event loop with an `await` legal on the same line. Paths themselves go through `path-ts` (`join`, `resolvePath`/`resolvePathBuilder`, `dirname`, `basename`, `extname`, `relative`, `normalize`, `isAbsolute`, `sep`), and a module's own location is `import.meta.dirname`/`import.meta.filename`; a `file:` URL that names something else converts through `@mailwoman/core/module/file-url`, and a package directory is located with `@mailwoman/core/module/resolvers`.
- **Data-root paths go through `@mailwoman/core/data-root`** — `dataRootPath("wof", "x.db")` / `mailwomanDataRoot()` (`packages/core/lib/data-root.ts`, using `path-ts` over the typed `$public` environment). `env-paths` supplies the platform default; lab checkouts provide `MAILWOMAN_DATA_ROOT` through private configuration. Never hard-code the lab path in shipped code or scripts. In docs/comments/help text reference `$MAILWOMAN_DATA_ROOT`, not the literal. Repo/package root paths are the sibling home `@mailwoman/core/paths`; both were extracted from `core/utils` in 2026-09 when it stopped being a home and became a shelf.
- **Never hand-assemble a path into another package's install directory.** `resolve(root, "node_modules/@mailwoman/neural-weights-en-us/model.onnx")` encodes a layout its owner never agreed to: it survives a hoist, a scope rename, a `files` change and a package move by silently pointing at nothing, and the caller reads that as "the artifact is missing" rather than "I looked in the wrong place". Three blessed mechanisms replace it — `resolveModulePath("pkg/file")` / `resolvePackageDirectory("pkg")` from `@mailwoman/core/module/resolvers` (or `createRequire(import.meta.url).resolve` where `import.meta` may not exist, e.g. under the Docusaurus config loader — `docs/plugins/demo-assets/resolve.ts` explains that case in place), a real `exports` subpath on the workspace, or `dataRootPath`/path-ts `resolvePath` when what you wanted was a data-root artifact. A package's OWN file — a data sibling `tsc` does not emit, a script to spawn, a test fixture — is `resolvePackagePath("<package>", …)`, anchored at the package root so it names the same file from the source tree, `out/`, and a published tarball; a repository file is `repoRootPath(…)`; a module's own directory is `import.meta.dirname`, and counting `..` up from it is refused by `mailwoman/no-import-meta-dirname-walk`. Every static or dynamic import of a sibling goes through the package's `imports` map (`#eval-harness/baseline-assert` — every workspace carries `"#*"`, so the path from the package root IS the specifier, with no extension), never `import("../…")` — `mailwoman/no-import-meta-resolve` and `mailwoman/no-relative-dynamic-import` hold those lines. One trap: importing `package.json` as a JSON module makes `tsc` copy it into `out/`, where it becomes Node's package scope for the compiled tree and sends every `#` import to `out/*.ts`; read the manifest with `readLocalJSONFile(resolvePackagePath(name, "package.json"))` instead. `packages/repo-health/lib/checks/node-modules-reacharound.ts` holds the line: it walks the TypeScript AST for a `node_modules` segment inside a `join`/`resolve`/`resolvePath` argument and carries a three-entry allowlist, each with its reason.

  **The rule is NOT "don't use `node:path`."** Most `join`/`resolve` call sites are legitimate — scratch directories, test fixtures, a CLI `--out` flag, walking a user-supplied tree, composing under a root a caller threaded in — and banning the import would flag all of them to catch a handful. Two shapes that look like violations and are not: a LOCAL root alias derived once from `mailwomanDataRoot()` and composed from thereafter (`packages/release-kit/lib/weights/copy-weights.ts` — the concern is re-deriving the root, not composing from it), and a probe that reads a FOREIGN project's install layout on purpose (`packages/release-kit/lib/release/smoke-clean-install.ts` — `resolveModulePath` would answer from the monorepo's graph, which is the exact thing a clean-install smoke must not consult).

- **Before writing a small utility, check whether it already has a home.** Most duplication here is not "nobody wrote it" but "the call site could not find it, or could not use it as shaped". The homes, beyond `APIClient` and `dataRootPath` above: The enforced half of this list is `HELPER_HOMES` in `oxlint.plugin.ts` (`mailwoman/prefer-home`): each row is a shape a re-typed copy cannot avoid (`toISOString().slice(0, 10)`, the literal `6371`, a generator's multiplier) with the import that replaces it, and the pre-commit hook reports a copy at commit time. When a review finds a helper typed twice, add the row; that is the durable half of the review. The detector for the shape nobody has tabled yet is `repo-health`'s `private-name-shadows-export` check: a module-private function sharing its name with a function another module exports is a copy or a collision, and a copy that stays says why behind `// repo-health-ignore private-name-shadows-export -- <reason>`.
  - `packages/core/lib/random.ts` (`@mailwoman/core/random`) — `mulberry32(seed)` and `makeLcg(seed)` as `() => number` thunks, plus the `SeededRandom` class. Sixteen files had re-typed one of these. Prefer `mulberry32` for new code; `makeLcg` exists only because its exact stream is baked into shipped corpus rows and eval splits.
  - `packages/core/lib/utils/sealed-db.ts` — `swapDatabaseIntoPlace(tmp, final)`, the atomic publish step this file specifies in prose, next to `sealDatabase` / `openBuiltDatabase`.
  - `packages/core/lib/stats.ts` (`@mailwoman/core/stats`) — `percentile` (nearest-rank, `p` in **[0, 100]**), `median`, `formatPercent`. Watch the unit: local copies elsewhere took a fraction, so a careless swap silently changes a promotion threshold.
  - `packages/core/lib/hash.ts` (`@mailwoman/core/hash`) — `sha256File` / `sha256Hex` / `md5File`. **There is no `core/utils/jsonl.ts`**, and this list named one until 2026-09-01: the module was deleted when `spliterator` replaced it, but its `./utils/jsonl` `exports` subpath outlived it by several releases, so `@mailwoman/core/utils/jsonl` was a published path that resolved to nothing (#2052). Reading a JSONL is `Array.fromAsync(JSONSpliterator.fromAsync<T>(path))`; file reading in general is `@mailwoman/core/fs/readers`.
  - `@mailwoman/spatial` — `haversineKm`, `pointInRing` / `pointInPolygon` / `pointInMultiPolygon`, the ring-list GeoJSON types with `pointInPolygonRings` / `geometryContains`, and `bboxAround`. Import them from here and nowhere else: several of them were defined in `resolver-wof-sqlite/geo.ts` despite having no SQLite dependency, which is how a gazetteer BUILD step ended up taking point-in-polygon from a query backend. That coupling is gone, along with the package's `./geo` subpath.
  - `packages/match/lib/comparators.ts` — `jaro`, `jaroWinkler`, `levenshteinSimilarity`, `nameSimilarity`, `jaccard`.
  - `packages/core/lib/utils/time.ts` — `isoDate(now?)` (`YYYY-MM-DD`), `isoSeconds` (`Z` suffix, second precision) and `isoSecondsUTC` (`+00:00`, the Python manifest shape). Twenty sites had re-typed `toISOString().slice(0, 10)` before `prefer-home` reported the shape; the same rule reports an Earth radius typed outside `@mailwoman/spatial`.
  - `packages/core/lib/git.ts` (`@mailwoman/core/git`) — `gitHead`, `currentBranch`, `dirtyTrackedFiles`, `trackedFiles`: the working tree's own git state, one `runFile("git", …)` each. Seven sites had spelled `git rev-parse HEAD` through their own wrapper; the `git-state` row in `HELPER_HOMES` reports the next one.
  - `packages/core/lib/fs/writers.ts` — `changeMode` for a chmod, `writePrivateTextFile` for a secret that must be created `0600`.
  - `packages/mailwoman/lib/cli-kit/metadata.ts` — `readMailwomanManifest()`, the one read of mailwoman's own `package.json` (version, engines floor, license).
  - `@mailwoman/core/release-config` — the typed reader of `release.config.json` and `repoCommittedSoftFeedSources`; `packages/release-kit/lib/weights/weights-recipe.ts` re-exports it. A reader that lives outside a package is unreachable from one, which is how a package comes to hardcode what the config names.
  - `@mailwoman/core/data-root` — `configRootPath` beside `dataRootPath`, for anything under `$MAILWOMAN_CONFIG_ROOT`.
  - `packages/corpus/lib/adapter.ts` — `HOUSE_NUMBER_PREFIX` + `splitStreetLine(line)`, the house-number/street split every US CSV adapter uses.

  Two leaf packages deliberately keep local copies rather than take a dependency: `nuts-lookup` and `timezone-lookup` re-implement the ray cast because reaching `@mailwoman/spatial` would pull `@mailwoman/core`'s ~11 MB of shipped data behind it, and `packages/match/gbt.test.ts` keeps an LCG for the same reason. Each says so in place. When you find a duplicate, price the dependency before moving it — the answer is sometimes "leave it, and write down why".

  **When two copies must agree, share the FUNCTION — sharing the constants proves nothing.** Matched literals and a "keep in lockstep" comment hold only the part of a contract a constant can express; the code diverges at the points it cannot — which field a term reads, whether a result is written back — and a reviewer comparing the constants finds them identical every time. Extract the function into a platform-free module and have both sides call it; `primary-preference.ts` and `proximity-rerank.ts` are the two that do.

  **A duplicate can indicate a bug in the shared tool.** When you meet one, the useful question is not "why didn't they know" — it is **"what was wrong with the shared thing?"** Sometimes nothing. Common causes include being too slow, async against a sync caller, or shaped for a different call site. Fix that and the duplicates collapse on their own; delete them without fixing it and they grow back.

- **A pty test must force Ink interactive.** `render()` resolves `interactive` from `is-in-ci` and `stdout.isTTY` (`resolveInteractiveOption`), and CI detection wins over a real TTY — so under GitHub Actions a probe driven through `script` renders NON-interactively, and a non-interactive Ink writes only the final frame at unmount. A test that reads a component's edit history out of the pty capture then finds exactly one frame: the last one, with the correct value. Pass `render(node, { interactive: true })` in the probe — `packages/mailwoman/lib/debug-view/test/input-probe.ts` is the worked example. The suppression costs wall-clock too: with nothing written before unmount, a `READY` wait burns its whole timeout on every case (`input.pty.test.ts`, 42s → 2.8s once forced).

- The **bare-import + subpath-import cycle** is a fragility surface. When a test file imports `@mailwoman/core` bare AND a subpath, Vite can leave the bare re-exports unbound while the slices interleave — base classes at the far side of the barrel evaluate as `undefined`. (`packages/core/lib/resources/libpostal.ts` had a top-level `await readdir` that contributed until #481 made it a lazy `getAvailableLanguages()` getter; the cycle turned out to be **structural** — Vite's bare/subpath interleaving — not TLA-driven, so it persists after the TLA removal.) Workaround: a side-effect `import "@mailwoman/core"` at the top of the affected test file forces full init first. Full fix = import-graph hygiene (tracked on #481).

### Moving a workspace

`tsc -b`, `yarn lint` and `yarn typecheck:tests` all pass over a half-moved tree. The paths that break are STRINGS the compiler never reads, each read at runtime by something that treats absence as a NEGATIVE ANSWER rather than an error: a `.husky/` regex that stops matching, a `run:` block, a CLI flag default, a test skip-guard. So after a move, sweep for QUOTED literals whose first path segment is a workspace name — not for imports, which the compiler already checks. Two shapes look stale and are not: a data-root-relative path (`data-bundles.ts`'s `localPath`, paired with a `remotePath`), and a package specifier — `import("mailwoman/gazetteer-pipeline")` is the `mailwoman` package's subpath export, not a directory, and prefixing it with `packages/` breaks it. CodeQL re-raises every existing alert at its new path and dismissals do not follow the file; re-dismiss after merge, and do not let the count block the merge — the ruleset requires the `test` context. The regroup that established this is in [`docs/records/retrospectives/agents-md-receipts.mdx`](./docs/records/retrospectives/agents-md-receipts.mdx).

## Database / inline SQL

If you are building a database, remember that they are readonly artifacts which should not be modified after creation. If the script builds a database, take care to build it successfully, then move the previous version to a temp directory, and then move the new version into place. This ensures that the database is always in a consistent state, even if the build script fails halfway through.

When making a database, use Kysley as the database connector. It is a thin wrapper around SQLite that provides a simple interface for creating and querying databases and is backed by the native `node:sqlite` module. It is the only supported database connector for this repo.
Table DDL goes through Kysely's schema-builder, not raw `db.exec("CREATE TABLE …")`. The idiom, established across #745–#749:

- A schema module owns both the typed `Database` interface AND a co-located `createXTable(db)` function built with `db.schema.createTable(...)`. The interface is the read/write contract; the builder creates the table; a column added to one is a compile error against the other. See `packages/resolver-wof-sqlite/{candidate,address-point,postal-city-candidate,postal-city-alias}-schema.ts`, `packages/resolver-wof-sqlite/lib/unified-schema.ts`, and `packages/tiger/lib/sdk/schema.ts`.
- `DatabaseClient` (`@mailwoman/core/kysley/client`) extends `Kysely` over `node:sqlite`. A build script constructs the raw `DatabaseSync` for its **hot positional INSERTs** (the bulk-load fast path) and wraps that _same_ handle in a `DatabaseClient` for the DDL — one connection, shared; `kdb.destroy()` owns the close. The schema-builder is async, so the DDL functions and their callers are `async`.
- `WITHOUT ROWID` has no first-class builder — use the ``.modifyEnd(sql`without rowid`)`` raw modifier. It's a win only for small-row, PK-probed tables (the candidate gazetteer, `pl_block`); never for a table carrying a large blob like geometry, where clustering the row into the B-tree _hurts_.

### What deliberately stays raw — don't "finish the job" on these

Some inline SQL is raw on purpose. If you migrate one of these thinking it was missed, you'll regress it. Each has a reason:

- **FTS5 virtual tables + `MATCH`** (`fts.ts`, `lookup.ts`, `extracts.ts`) — Kysely can't express `CREATE VIRTUAL TABLE … USING fts5` or the `MATCH` operator.
- **ogr2ogr / GDAL-dialect SQL** (`packages/tiger/lib/sdk/fetch.ts`) — runs inside ogr2ogr against shapefiles, not the app DB.
- **Hot bulk writes** — the positional prepared-statement INSERT loops, their `BEGIN`/`COMMIT`, and the candidate clustering `INSERT … SELECT … ORDER BY`. Plus `PRAGMA`, `VACUUM`, `ANALYZE`, `ATTACH` — none are Kysely-modelled, and the inserts are the throughput path.
- **Runtime-dynamic schemas** (`packages/corpus/scripts/ingest-csv.ts`) — columns + types are inferred from the CSV at runtime; a builder loop wraps the same dynamic strings with ceremony and no added type safety.
- **Introspect-and-replay** (`packages/resolver-wof-sqlite/lib/build-slim.ts`) — it execs the _source_ DB's own `CREATE TABLE` strings read from `sqlite_master`; a static builder can't express a copied schema.
- **Async-into-sync walls** — `PlacetypeDataSource.ts` runs its DDL in a synchronous class constructor; `zcta-centroids.ts` and `coincident-roles.ts` are sync, heavily-tested helpers (the latter behind a sync CLI). Kysely's builder is async, so migrating cascades `async` through a sync call graph and rewrites the tests, all for one small table — not worth it.
- **Sync-by-interface resolver readers** — the resolution ladder is synchronous by design: `AddressPointLookup.find()`, `InterpolationLookup.find()`, `PostcodeResolver.lookup()`, and `ConventionSource.get()` (`packages/core/lib/resolver/types.ts`) return values synchronously, and `PostcodeResolver` is called from `@mailwoman/neural`. So the readers implementing them — `address-point.ts`, `interpolation.ts`, `postcode-point-lookup.ts`, `sqlite-convention-source.ts`, `ancestry.ts`, and the hot per-keystroke `candidate-lookup` probe — stay on raw `.prepare()`. Converting one to async Kysely doesn't cascade a few call sites; it forces the shared interface async, rippling through every implementer and across the package boundary — a structural architecture change, not a query swap. (#752's `postal-city-alias` reader was the one exception: not interface-bound, and its sole caller was already inside the async `findPlace`.)

On the query side, the migratable remainder is the cold, already-`async` `SELECT`/`INSERT` sites in the build / eval / corpus scripts — typing there is a free win with no hot-path or interface concern. The DuckDB builders need no `@oorabona/kysely-duckdb` dialect: their SQL is `read_parquet`/`ST_Read` (raw); only the SQLite output tables migrate, and those reuse the shared schema builders (see `build-postal-city-alias.ts`).

## When in doubt

Read the workspace-local docstrings before changing infrastructure files. The headers in `packages/release-kit/lib/**`, `packages/repo-health/lib/**`, `.release-it.json`, and `.github/workflows/*.yml` explain why each piece exists. There is no root `scripts/` directory: an executable enters through a registry (`@mailwoman/release-kit`, `@mailwoman/repo-health`), a `mailwoman` command, or a `packages/mailwoman/lib/dev-tools/*.run.ts` measurement, and the `no-root-scripts` health check refuses a path that names one. If a file's purpose isn't documented inline and you're about to touch it, add the docstring as part of your change — future-you (or future-claude) will thank you.

**A reader that can return a PARTIAL result must say what it got, or throw.** Every failure of this shape returns a well-formed wrong answer that is indistinguishable from a real absence — and absence is the result the question seeks, so the wrong answer arrives looking like a discovery. Three rules follow. Prefer a reader that ERRORS on a requested-but-missing column over one that omits it. When a projection drops a field, the consumer that needed it gets `unchanged`, not a type error, so carry the whole payload or name what you dropped. And `?? 0`, optional chaining and a bare `.filter()` all convert _"I could not read this"_ into _"there is none of it"_ — at a measurement boundary that is a lie, and the cheap insurance is to re-measure a surprising ABSENCE by a second path before reporting it. The seven shipped instances that produced these rules are in [`docs/records/retrospectives/agents-md-receipts.mdx`](./docs/records/retrospectives/agents-md-receipts.mdx).

**The defects that survive review live in the input tail.** Reading code tells you what it does on the inputs you are imagining, which is exactly the set that already works: a ReDoS that needs tab characters, a leading zero dropped only for base cells 0–7, a quote-blind CSV splitter against sources that happen to carry no embedded newline, a quadratic scan whose every existing caller happened to be chunk-bounded. So when a change rests on a claim about data or scale — _this source has no quoted newlines_, _this path is fast enough now_, _this artifact is stale_ — spend the one command that determines the result, and put the number in the docstring afterwards: `27,405 ms → 175 ms on 8 MB` tells the next reader whether the constraint still binds; "this is slow" does not.

Corollary for the docstring you write afterwards: if the claim was established by measurement, put the number in. `27,405 ms → 175 ms on 8 MB` tells the next reader whether the constraint still binds. "This is slow" does not.

That corollary covers a MEASUREMENT THAT STILL BINDS, and nothing else. A comment states the invariant, the non-obvious constraint, or the reason an obvious implementation is unsafe; the incident that produced it goes in the commit message, the PR, or the runbook page. **A date, a version number, or a list of affected packages inside a comment is the tell** — strike it and see whether the sentence still stands; if it does not, the sentence was history.

## Addendum

- We use a version of Node.js that can strip types without any additional CLI flags. This is appropriate for everything but the Ink commands, which are TSX and require compiling.

### Acronym casing in identifiers

Acronyms are capitalized as whole camelCase components: `createWOFResolver`, `parseJSON`, `readID`, `fetchOSM`, `WOFSQLitePlaceLookup`, `modelURL`. Not `createWofResolver` / `parseJson` / `readId`. This applies to TS/TSX identifiers (functions, classes, types, interfaces, object properties) and the code examples in living reference docs. It does **not** touch:

- **`snake_case`** — DB columns and wire keys (`parent_id`, `place_id`, `name_key`) stay as-is; they're string contracts, and the convention is camelCase-only by construction.
- **External library names** — match the dependency's own casing (`HttpStatusCode` from axios, `createQueryId`/`createWithSql` from kysely, `sqlite-wasm`).
- **CLI flag props** — the schema adapter binds a kebab flag to a lowercase-acronym prop (`--resolve-db` → `resolveDB`), so those specific schema keys must match its derivation. Result-read fields and domain properties (`result.placeID`) still follow the convention.
- **Dated historical records** — `docs/records/evals/`, `reviews/`, postmortems, and phase docs are point-in-time; don't rewrite their acronyms.

`yarn lint` enforces this via `sister-software/no-title-case-acronym` from `@sister.software/oxlint-config`. It reads the AST, so it sees exported declarations, the MEMBERS of exported interfaces/classes/enums, and the exported half of a re-export — the standalone script it replaced matched `^export <keyword> <name>` and missed every member. Configure it in `oxlint.config.ts`: `extraAcronyms` layers this project's vocabulary onto the shipped list, `ignoreNames` exempts names whose casing is not ours. Prefer a scoped `// oxlint-disable-next-line sister-software/no-title-case-acronym -- <reason>` at the one site that needs it; a config entry silently covers every future declaration of that name. When adding an acronym, cap the whole component.
