# Mailwoman Neural — work log

Append-only. One line per meaningful event. Format:
`YYYY-MM-DD HH:MM | phase-N | <what was done> | <next>`

2026-05-16 18:20 | phase-0 | session start, read epic #15 + sub-issues #3–#8 via unauthed GitHub API | bootstrap meta-files, begin yarn workspaces restructure
2026-05-16 18:24 | phase-0 | verified `yarn compile && yarn test` green (771/771); clean-clone preflight needs the explicit compile step (noted in DECISIONS.md) | task 1: workspaces
2026-05-16 18:46 | phase-0 | yarn workspaces: moved core/utils/solvers/filters into @mailwoman/core, classifiers into @mailwoman/classifiers; hoisted utils/parser.ts + DebugOutputBuilder.ts + commands/ back to root to break circular dep; sdk/repo.ts moved into @mailwoman/core/utils; node_modules/mailwoman self-symlink added for residual root-only paths; 109 import sites rewritten to @mailwoman/\* via scripts/rewrite-workspace-imports.mjs; project references in tsconfig wire build order; full suite green (771/771) | task 2: ComponentTag schema
2026-05-16 18:50 | phase-0 | added COMPONENT_TAGS + ComponentTag + BIO_LABELS + BioLabel in @mailwoman/core/types (packages/core/core/types/component.ts), 9 schema tests pass, total 780/780 | task 3: ClassificationProposal + Classifier interface + legacy adapter
2026-05-16 18:55 | phase-0 | core types: ClassificationProposal, ProposalClassifier, ClassifierContext, Section alias; mapping.ts (legacy Classification → ComponentTag); wrapLegacyClassifier in @mailwoman/classifiers; equivalence test on HouseNumberClassifier passes; total 784/784. DEFERRED in this commit: building the registry that wraps every rule classifier, plumbing proposals into the solver path (logged in DECISIONS as task-3 follow-up). | task 4: PolicyRegistry
2026-05-16 18:59 | phase-0 | @mailwoman/core/policy: PolicyMode union, ClassifierPolicy / PolicyRegistry interfaces, defaults (rule_only for every COMPONENT_TAG), InMemoryPolicyRegistry with .set/.remove/.lookup/.apply; 14 mode + threshold + locale-specificity tests pass; total 798/798. Solver wiring still on the legacy mutation path (same task-3 follow-up deferral). | task 5: LocaleProfile + en-US/fr-FR
2026-05-16 19:02 | phase-0 | @mailwoman/core/locale: LocaleProfile + LocaleRegistry; en-US, fr-FR, and ja-JP profile constants (componentsSupported per #5/#8); InMemoryLocaleRegistry validates componentsSupported ⊆ COMPONENT_TAGS and that policy overrides stay within supported components; 12 tests cover registration, validation, the three bundled profiles, and the JP forward-compat invariant; total 810/810 | task 6: CLI --locale flag
2026-05-16 19:09 | phase-0 | CLI: --locale <bcp47> flag on parse + debug commands (BCP-47 regex via zod), ParseOptions.locale accepted by AddressParser (not yet acted on); test/locale-flag.test.ts spawns the compiled CLI and verifies en-US + fr-FR exit 0 and output is locale-stable in Phase 0; total 817/817 | task 7: JP forward-compat + final phase-0 criteria
2026-05-16 19:11 | phase-0 | JP forward-compat (test/phase-0-forward-compat.test.ts, 6 cases): ja-JP componentsSupported ⊆ COMPONENT_TAGS; LocaleRegistry holds en-US/fr-FR/ja-JP simultaneously; neural-only locale expressible (empty ruleClassifiers); PolicyRegistry handles JP-only tags (prefecture etc.) with and without locale overrides; total 823/823 | phase-0-complete (modulo deferred task-3 follow-up: full wrapLegacyClassifier registry + solver rewire, and operator-side merge + tag)
2026-05-16 19:11 | phase-0 | PHASE-COMPLETE: lint clean, 823/823 tests pass, CLI golden path emits matching output for `Mt Tabor Park, 6220 SE Salmon St, Portland, OR 97215, USA`, `--locale fr-FR` parses the Paris sample, JP forward-compat sanity green. Schema, ClassificationProposal/ProposalClassifier contracts, PolicyRegistry, LocaleRegistry, en-US/fr-FR/ja-JP profiles, and the CLI flag are in. Phase 1 (corpus) is unblocked. Solver-side proposal consumption + full per-classifier wrap is a follow-up commit, NOT a phase blocker — see DECISIONS.md task-3 entry. | hand back to operator: merge + tag neural-phase-0-complete
2026-05-17 01:10 | phase-1 | session start on issue #9 (Phase 1 corpus pipeline); workspace + @mailwoman/corpus scaffold + types per #6 (CanonicalRow, LabeledRow, CorpusAdapter, AdapterOptions, SourceProvenance, SynthMarker, QuarantinedRow); packages/corpus-python skeleton (standalone, !packages/corpus-python in workspaces). 829/829 tests | task 2: adapter framework
2026-05-17 01:17 | phase-1 | adapter.ts (AdapterRegistry, InMemoryAdapterRegistry, defaultAdapterRegistry, stableSourceId, canonicalDedupKey, streamingSha256); runner.ts (runAdapter, runAllAdapters with backpressure-aware JSONL output, dedup, sha256, MANIFEST.json, AbortSignal); CLI: `mailwoman corpus run <adapter-id> --input --output [--country --limit]` + `corpus list`. 856/856 | task 3: formatAddress utility
2026-05-17 01:22 | phase-1 | format.ts (toOpenCageComponents + formatAddress + composeRoad / composePostcode); bridges ComponentTag dict → OpenCage vocab via @fragaria/address-formatter (root dep promoted to corpus); separator option for single-line vs newline output; cedex / unit / intersection handling. 870/870 | task 4: wof-admin adapter
2026-05-17 01:30 | phase-1 | wof-admin adapter (CC0); reads `spr` table from a WOF spatial.db; walks parent_id ancestry; emits self / with-region / with-region-country variants per locality+region+country chain; subregion = self only; country uses canonical OpenCage display name to align with template auto-expansion; fixture.sql (US Portland/Burlington/Vermont, FR Paris/Lyon/Île-de-France/Auvergne-Rhône-Alpes/Rhône) materialized at test time; CLI `corpus list` shows wof-admin. 881/881 | task 5: wof-postalcode + reconcileComponents
2026-05-17 01:35 | phase-1 | wof-postalcode adapter; emits up to 4 variants per postcode (self / with-locality / with-locality-region / with-locality-region-country); reconcileComponents() helper drops components whose surface forms don't appear in raw (FR drops region when postcode present, US abbreviates "Oregon" → "OR"); applies to both wof adapters. 889/889 | task 10: alignment
2026-05-17 01:40 | phase-1 | tokenize.ts (Tokenizer + TokenSpan, whitespaceTokenizer Unicode-aware); align.ts (alignRow: substring + fastest-levenshtein fuzzy fallback, claimed-spans bookkeeping for ambiguous values, BIO label assignment, quarantine with reason); fastest-levenshtein added as corpus dep. 906/906 | task 12: synthesis
2026-05-17 01:42 | phase-1 | synthesize.ts (Augmentation interface, withAugmentation helper); universal augmentations (case-upper/lower, drop-commas, double-space, accent-strip); US (state-expand/abbreviate via 50-state map, directional-expand/abbreviate, zip-plus4-dash-drop); FR (particle-strip); AUGMENTATIONS registry + defaultAugmentationsForCountry. 927/927 | task 14: eval splits
2026-05-17 01:44 | phase-1 | split.ts (locality holdout US: VT/WY/ND, FR: Corse/Lozère/Creuse); 50/50 val/test by sha256 source_id bucketing; writeSplitManifests writes sorted train/val/test.txt + SPLIT_MANIFEST.json (diff-friendly, reproducible); DECISIONS.md entry documenting the holdout rationale. 939/939 | task 13: parquet output
2026-05-17 01:47 | phase-1 | parquet.ts (writeShards: per-split JSONL shards at <corpus-v…>/<split>/part-NNNN.jsonl, 1M rows/shard, streaming sha256, MANIFEST.json with per-shard descriptors); rowToParquet flattens synth marker; companion Python jsonl_to_parquet.py converter (PyArrow, zstd, 50k row groups) at packages/corpus-python/scripts/. 946/946 | task 16: build pipeline command
2026-05-17 01:50 | phase-1 | build.ts (buildCorpus orchestrates adapters → synth → align → split → parquet); top-level MANIFEST.json + intermediate/labeled.jsonl + quarantine.jsonl + splits/ + corpus-v<v>/; CLI `corpus build --corpus-version --output --inputs '{...}'` (NOT `--version`, reserved by Pastel). Smoke run against wof-admin fixture: 24 canonical → 110 labeled (synth fan-out), 5 quarantined, split 98/5/7. 950/950 | task 7: BAN adapter
2026-05-17 01:53 | phase-1 | BAN adapter (FR street-level, ODbL); streams CSV via csv-parse (promoted to corpus dep); house_number = numero + rep (bis/ter); fixture sample.csv with Paris/Lyon/Marseille/Nice. 956/956 | task 11: tokenizer training + task 15: golden eval
2026-05-17 01:57 | phase-1 | packages/corpus-python/scripts/train_tokenizer.py (SentencePiece unigram, vocab=16000, char-cov=0.9995, byte-fallback, META.json output); data/eval/golden/v0.1.0/ seed (10 US + 10 FR entries + README + 1000-entry contribution workflow); golden.ts validator (schema + reachability checks via reconcileComponents); validateGoldenDir confirms seed has zero issues. 965/965 | hand back to operator: P2 osm-places + openaddresses + osm-addr adapters and P3 US-gov + SIRENE remain (skip-or-Phase-2 per the plan); operator runs corpus build against real WOF / BAN data and tags neural-phase-1-complete
2026-05-17 02:33 | phase-1.5 | session start on issue #18 (P2 adapters + isp-nexus salvage + JS-native Parquet); `/home/lab/Projects/isp-nexus/universe` not in container — operator question filed via outbox (.playpen/control/outbox/20260517023313859-3eb9.json) on access strategy for §2 (codex/TIGER/H3) + §4 (Parquet wrapper + yarn patch). Starting §1 OpenAddresses adapter in parallel (no isp-nexus dep). | OpenAddresses adapter
2026-05-17 02:54 | phase-1.5 | openaddresses adapter (CC-BY-4.0 default; per-row LICENSE override); ND-GeoJSON streaming via node:readline (one Feature per line); maps NUMBER/STREET/UNIT/CITY/REGION/POSTCODE → house_number/street/unit/locality/region/postcode (case-insensitive property keys); `district` intentionally unmapped; --country REQUIRED (OA files are country-partitioned but rows lack country code); fixture sample-us.geojson 6 rows mixing CC-BY-4.0/PDDL-1.0/CC0-1.0/CC-BY-SA-4.0/no-license to exercise license propagation. CLI `corpus build --inputs` schema widened to accept `Record<string, string | AdapterOptions>` so the country flag plumbs through; backwards-compatible (existing path-string callers untouched). End-to-end smoke: 6 canonical → 38 labeled (synth fan-out: case/comma/double-space + US directional/zip), 0 quarantined. .prettierignore extended for line-delimited GeoJSON fixtures + .playpen control protocol files. 976/976 | hold §2/§4 pending operator answer on isp-nexus; success criterion "at least one of osm-places/openaddresses/osm-addr" satisfied
2026-05-17 03:02 | phase-1.5 | operator clarification: option-1 bind-mount at /srv/isp-nexus/ (RO). Sequencing approved §4 → §2.1 → §2.3. AGPL→AGPL direct copy clean; preserve  attribution in headers;
2026-05-17 03:20 | phase-1.5 | §4 JS-native Parquet — salvaged isp-nexus sdk/parquet/{schema,writer,reader,index}.ts (~252 LOC, AGPL-3.0) into packages/corpus/src/parquet-wrapper/ with two trims: dropped @mailwoman polyfill (Node 22 native Promise.withResolvers), replaced path-ts PathBuilderLike with string|URL. Yarn patch @dsnp-parquetjs-npm-1.7.0-efe8288b39 brought over verbatim (removes @aws-sdk/client-s3 + several @types/* from runtime deps to devDeps; saves ~50MB). Compression: SNAPPY (lib doesn't support zstd — divergence documented in DECISIONS.md; SNAPPY is PyArrow's default codec). LABELED_ROW_SCHEMA in parquet.ts defines optional locale/synth_*; rowsPerShard=1M, rowGroupSize=50k per spec. `writeShards` rewritten: opens ParquetWriter per (split, shardIndex), appendShape omits nulls so optional columns honor parquet definition levels, file sha256 computed by re-read after close. Test rewrite: round-trip via ParquetReader (5 new + 2 schema tests); build.test.ts also rewired to ParquetReader. Deleted jsonl_to_parquet.py + pyarrow dep from packages/corpus-python/pyproject.toml. End-to-end CLI smoke: 6 OA rows → 38 labeled → 1 part-0000.parquet (7211 bytes SNAPPY). 981/981. | task §2.1 USPS Pub-28 suffix codex
2026-05-17 03:30 | phase-1.5 | §2.1 USPS Pub-28 street-suffix codex — salvaged isp-nexus mailwoman/postal/suffix.ts dataset (~180 canonical → variants entries, AGPL-3.0) into packages/corpus/src/codex/us-street-suffix.ts; dataset verbatim from Pub-28 Appendix C, helper API reshaped for synthesis (matchCase + matchTrailingSuffix) since the original was parsing-oriented. Two new augmentations in synthesize.ts: us-street-suffix-abbreviate (Avenue→Ave, AVENUE→AVE, Street→St, accepts non-preferred variants like AV) and us-street-suffix-expand (inverse: Ave→Avenue, AV→AVENUE, STRT→STREET). Case-pattern preserved via matchCase (UPPER / lower / Title). Operates on trailing word of components.street only (avoids mangling streets like "Avenue of the Americas"). Wired into AUGMENTATIONS registry + defaultAugmentationsForCountry("US"). 14 new tests (12 augmentation cases + 2 registry/default checks). Closes the Phase 1 gap the operator called out: "Phase 1 shipped directional / state / ZIP-dash but did NOT cover street suffix expansion." Side-fix: added LOG.md to .prettierignore (prettier markdown was corrupting historical entries by interpreting bare underscores as italic markers). 995/995. | task §2.3 TIGER adapter
2026-05-17 03:37 | phase-1.5 | §2.3 TIGER adapter — salvaged FIPS-state lookup from isp-nexus tiger/state.ts (50 states + DC + 5 territories, AGPL-3.0) into packages/corpus/src/codex/us-fips-state.ts. Did NOT carry over the TypeORM/service-layer scaffolding (~3000+ LOC across tiger/sdk/, geoid.ts, county.ts, etc.) — the mailwoman adapter contract is async-generator-of-CanonicalRow, not an ORM-backed DB layer. New adapter packages/corpus/src/adapters/tiger/ consumes a SQLite DB the operator pre-builds from raw TIGER shapefiles via ogr2ogr (docs in README) against two tables (tiger_streets + tiger_places mirroring TIGER's ADDRFEAT + PLACE column names). Emits two row classes: street-level (street + region + 0-2 postcode variants per zipl/zipr setup) and locality-level (3 variants matching wof-admin's locality-only / with-region / with-region-country fan-out). License: "Public Domain" per Census Bureau (no per-row override needed). 10 new tests + fixture (7 streets + 4 places across OR/VT/WY/CA; WY held out per split policy so val/test populate during integration smoke). End-to-end CLI smoke: 19 canonical → 106 labeled, 6 quarantined, splits populated train/val/test (Wyoming rows correctly routed to held-out splits), output writes .parquet (suffix codex augmentations also compose: "Salmon St" → "Salmon Street", etc.). 1005/1005. | success criteria for issue #18 container-side work complete: §1 openaddresses + §2.1 USPS suffix codex + §2.3 TIGER + §4 JS-native Parquet all shipped. Operator-side remaining: real-data corpus build + golden set ≥500 + neural-phase-1-complete tag.
2026-05-17 18:14 | corpus-v0.1.0 | session start on ad-hoc task `task-real-data-corpus-build-v0-1-0`. Pre-staged sources at /data/corpus/sources/: BAN 5 départements (75 Paris / 13 Bouches-du-Rhône / 2A Corse-du-Sud / 48 Lozère / 69 Rhône, 1.08M rows merged via tail-concat); TIGER pre-built SQLite (361k streets + 791 places); WOF clones — admin-fr 122k features, postalcode-fr 27k, postalcode-us 42k. WOF admin-us arrived as partial clone (`partialclonefilter=blob:none`) with 27 fixture blobs materialized; bulk re-fetch via shallow `git clone --depth=1` from github.com/whosonfirst-data/whosonfirst-data-admin-us (~4.8G working tree, 293k admin features). 1009/1009 baseline tests pass post-install. | run build
2026-05-17 18:35 | corpus-v0.1.0 | end-to-end build complete — `mailwoman corpus build --corpus-version 0.1.0 --no-synthesize --output /data/corpus/versioned/v0.1.0 --inputs '{...}'`. Adapter totals (canonical rows written → deduped → elapsed): wof-admin 3,296,971 ← 4,163,753 / 866,782 deduped / 9m53s; wof-postalcode 103,716 / 1m43s; ban 1,082,500 / 18s; tiger 79,031 ← 378,527 yielded / 299,496 deduped / 48s. Total 4,562,218 canonical. Alignment: 66 quarantined (0.0014%) — all `component-not-found:*` on WOF localized `name:*` properties carrying internal double-spaces ("டெரரிடோனிரே  டி  பெலஃபோர்ட்" Tamil for Territoire de Belfort, etc.) that the single-space `formatAddress` template can't substring-match. Splits via locality holdout: train 4,444,546 (97.4%) / val 58,840 (1.29%) / test 58,766 (1.29%). BAN rows lack `region` so they default to `train` (documented adapter limitation) — that is the dominant skew from the 90/5/5 nominal target. Output: 5 train + 1 val + 1 test parquet shard at /data/corpus/versioned/v0.1.0/corpus-v0.1.0/ (~157M SNAPPY), full top-level MANIFEST.json with adapter manifests + split holdouts + shard counts + sha256s. Synth-OFF: see DECISIONS.md entry for the rationale (8G container RAM cap + in-memory splitInputs array would have OOM'd at ~4× US synth fan-out). openaddresses adapter intentionally skipped (no OA dump pre-staged in this container). 1009/1009. | hand back to operator: artifacts at /data/corpus/versioned/v0.1.0/, ready for golden set ≥500 + neural-phase-1-complete tag
2026-05-17 19:10 | corpus-v0.1.1 | splitInputs refactor: build.ts + split.ts + parquet.ts (+ tests) — eliminated three O(n_aligned_rows) in-memory structures: splitInputs array (build.ts), SplitManifest.{train,val,test} arrays (split.ts), splitByIdMap (build.ts). New shape: splitForRow pure helper decides each row's split inline at align time; per-split labeled JSONL streams (labeled-{train,val,test}.jsonl) routed inline; writeSplitManifestsFromLabeledFiles streams + shells out to `sort(1)` from coreutils for deterministic .txt manifests; writeShards now takes PerSplitRows (Partial<Record<SplitName, AsyncIterable<LabeledRow>>>) and drops the splitFor callback. ~50 LOC source + ~30 LOC tests across 3 files; 1015/1015 tests pass (+6 new — splitForRow ×3, writeSplitManifestsFromLabeledFiles ×2, writeShards skip-empty ×1). Memory: synth-ON v0.1.1 retry build peaked at ~3.9 GiB during wof-admin adapter (ancestry index resident); dropped to ~700-800 MiB during align + shard phases — the refactor's ~600 MiB target met. Without the refactor synth-ON would have OOM'd at ~5 GiB+ for splitInputs alone. | run synth-ON v0.1.1
2026-05-17 19:35 | corpus-v0.1.1 | first synth-ON v0.1.1 build attempt completed but flagged 2,006,426 quarantined rows (8.94%). Drill-down: 2,004,682 (99.9%) traced to the `double-space` augmentation — it doubled spaces in `raw` but left `components` single-spaced; alignment substring-search then couldn't find e.g. components.street="Chemin des Amandiers" inside raw="...Chemin  des  Amandiers...". synthesize.ts:doubleSpace bug fixed: also double-space each component value. Regression test in synthesize.test.ts asserts the substring invariant (every component value must appear in raw). | re-run v0.1.1
2026-05-17 19:39 | phase-1.6 | session start on issue #22 (adversarial sources + compositional synthesis). Operator inbox confirmed lane discipline: don't touch build.ts / split.ts / parquet.ts (v0.1.1 sibling container in flight on `task-real-data-corpus-build-v0-1-0`). Sequencing §1.1 → §2.1 → §1.2 → §3 per issue recommendation. | §1.1 FCC BDC adapter
2026-05-17 19:39 | phase-1.6 | §1.1 FCC BDC adapter — pivot on salvage source: `BroadbandProvider` + `BSLAvailability` in `/srv/isp-nexus/fcc/bdc/` carry NO addresses (provider metadata + speed records keyed by `location_id`). The address-bearing entity is `NTIARecord` in `fcc/bdc/data-collection.ts` — `address_primary` + `city` + `state` + `zip` + `zip_suffix`, the NTIA-conformant public subset of the BSL fabric. Adapter consumes a SQLite DB with one `bdc_locations` table modeled on NTIARecord (operator pre-builds via isp-nexus BDC ETL or equivalent host-side script). Splits address_primary into house_number + street; combines zip + zip_suffix into postcode. One CanonicalRow per location_id (no fan-out — fabric records are already fully-specified addresses, variants come from §2.1 compose). Codex extension: `lookupStateAbbreviation` reverse view added to `us-fips-state.ts` so adapters carrying 2-char USPS abbreviations can validate without a FIPS round-trip. License: "Public Domain". 21 tests including kryptonite + sha256 determinism. 1006 → 1026 corpus tests (full repo 1086). | §2.1 compose primitive
2026-05-17 19:39 | phase-1.6 | §2.1 composeAdversarialRow — new binary primitive in synthesize.ts. Signature `(venue: string, address: CanonicalRow, opts) -> ComposeResult` (NOT an Augmentation; lives in synthesize.ts but outside AUGMENTATIONS registry). Emits LabeledRow directly: venue tokens unconditionally B-venue/I-venue; address half re-uses labels from aligning the input address in isolation. Deterministic boundary — no re-search across the venue/address seam, so embedded place-shaped tokens (Buffalo Health Clinic, …, Buffalo NY) stay correctly labeled. 11 tests cover all three issue-spec kryptonite patterns (place-name-venue, place-shaped-venue, particle-honorific) + provenance + separator option + quarantine propagation + determinism. DECISIONS.md entry argues why compose is a separate primitive (different arity, different output shape, two-source provenance). | §1.2 HRSA + SAMHSA
2026-05-17 19:39 | phase-1.6 | §1.2 HRSA + SAMHSA adapters — usgov-hrsa-fqhc consumes the HRSA Health Center Service Delivery Site Locations CSV (column names match data.hrsa.gov export header); usgov-samhsa-treatment-locator consumes the SAMHSA Behavioral Health Treatment Services Locator CSV (joins name1+name2 into venue with ` - `, joins street1+street2 with `, ` for two-line addresses). BOTH adapters emit the `venue` component ahead of address components in the components dict — alignment claims venue span first (load-bearing insertion order; documented in inline comments). 12 tests each (24 total) including kryptonite-row verification via `alignRow` that the embedded "Buffalo" lands B-venue not B-locality. CSV-fed adapters use `fixtures/<id>/sample.csv` consumed directly by csv-parse (already a corpus dep). License: "Public Domain". | §3 adversarial eval set
2026-05-17 19:39 | phase-1.6 | §3 adversarial eval set — `data/eval/golden/v0.1.0/adversarial.jsonl` with 54 hand-labeled entries across 9 categories: kryptonite/{place-name-venue, place-shaped-venue, particle-honorific, disambiguation} + graceful/{typo, mis-casing, mis-punctuation, whitespace, no-commas, label-prefix, contamination, trailing-junk, attention, unit, country}. Each entry's `notes` field begins with `<category>/<subtype>:` for stratification and explicitly documents what the model should ideally do (including partial-parse + low-confidence when full-confidence-but-wrong is the failure mode to avoid). Schema-compatible with v0.1.0 (no version bump needed; all surface forms reachable in raw via reconcileComponents). README.md updated with category cheatsheet. golden.test.ts assertion updated from 2-file/≥20-entry baseline to 3-file/≥70-entry baseline. | hand back to operator
2026-05-17 19:40 | phase-1.6 | PHASE-COMPLETE: 237 corpus tests pass (181 → 237, +56 from this issue), full repo 1065/1065. Lint clean (prettier + eslint). Commit 6b902fdb pushed to bind-mounted bare repo. Lane discipline audited: no touches to build.ts / split.ts / parquet.ts (v0.1.1 sibling container's hot files). Out-of-scope deferred per issue: §1.3 CMS NPI (multi-GB, defer until federal subset proves out), §1.4 state-level (50 schemas, defer per issue + per operator inbox guardrail). Session notes at `.playpen/session-notes.md`; post-mortem at `.playpen/control/postmortem.md`. Status: ready. | hand back to operator: review + PR
2026-05-17 20:14 | corpus-v0.1.1 | end-to-end build complete (retry with doubleSpace fix) — 22,439,941 aligned rows (4.92× synth fan-out vs v0.1.0's 4.56M canonical), 8,970 quarantined (0.040% — 99.9% reduction from the 8.94% rate of the first attempt). Per-adapter quarantine: wof-admin ~7-8k double-space-amplified WOF whitespace artifacts (same root cause as v0.1.0's 66 cases — components carrying multi-space artifacts from `name:*` properties that `formatAddress` collapses in `raw`), small leftover slices from state-abbreviate (838) / drop-commas (720) / case-upper-lower / accent-strip. Splits 21,836,554 train / 301,267 val / 302,120 test. 24 parquet shards (22 train + 1 val + 1 test) at /data/corpus/versioned/v0.1.1/corpus-v0.1.1/, ~835 MiB SNAPPY. RSS peak ~3.9 GiB during wof-admin adapter (ancestry index dominated), ~700-800 MiB through align/split/shard — well under the 5.8 GiB heap cap. Wall-clock: adapters 14m (wof-admin 10m47s / wof-postalcode 1m41s / ban 17s / tiger 49s), align ~10m, split + shard ~5m, total ~29m. | hand back to operator: v0.1.1 ready for review; container can be destroyed or held for follow-up
2026-05-17 21:21 | phase-2 | session start on issue #10 (Phase 2 training). Pre-flight audit: corpus-v0.1.0 (4.56M rows, synth-OFF) + corpus-v0.1.1 (22.4M, synth-ON) on disk, golden set v0.1.0 in-repo at 74 entries (10 US + 10 FR + 54 adversarial; far short of the 500+500 target), tokenizer-v0.1.0 NOT YET TRAINED (Phase 1 leftover — script existed but had not been run against real corpus), /data/models/{checkpoints,onnx,quantized}/ empty. Hardware: no GPU, 8G RAM, 4 CPU, 814G disk free. Scope decision for this session: scaffolding + smoke train; full GPU training is operator handoff. | train tokenizer
2026-05-17 21:23 | phase-2 | tokenizer-v0.1.0 trained — SentencePiece unigram, vocab=16000, char-cov=0.9995, byte_fallback=true. Sample: 500k US + 500k FR raws from corpus-v0.1.0 train split via new `scripts/sample_balanced_raws.py` (reservoir sampling, column-projected reads to keep RSS low). Output at `/data/models/tokenizer/v0.1.0/{tokenizer.model,tokenizer.vocab,META.json}`. sha256 da351fe3...41856. Sanity probes: 13 sub-tokens for the White House address, 9 for 10 Rue de la République, byte_fallback handles CJK input. | scaffold mailwoman_train
2026-05-17 21:38 | phase-2 | `packages/corpus-python/src/mailwoman_train/` package landed — subcommand CLI `python -m mailwoman_train <cmd>` (train / eval / export / quantize / package / smoke / verify-tokenizer). Modules: labels.py (Stage 1 coarse 15-label BIO set: O + 7 tags × {B-,I-}), tokenizer.py (SentencePiece wrapper with byte→char span realignment via `EncodeAsImmutableProto`), config.py (YAML loader with Config/DataConfig/ModelConfig/TrainConfig/EvalConfig dataclasses), data_loader.py (streaming parquet via PyArrow row groups, country-weighted reservoir-style acceptance, coarse-presence filter, length-cap, tokenizer-alignment verifier), model.py (BertForTokenClassification builder, from-scratch init, 6L/256H/4 heads/1024 FF/128 maxpos), train.py (AdamW + linear-warmup-cosine schedule + fp32/fp16/bf16 toggle + CSV logging + checkpoint-every-N), eval.py (golden-set runner with per-component F1 / full-parse EM / mean-conf / calibration buckets, markdown + JSON reports), export_onnx.py (dynamo=True path with onnxscript, dynamic axes batch+sequence, parity check vs PyTorch over N val samples), quantize.py (onnxruntime.quantization dynamic int8), package_weights.py (writes packages/neural-weights-{en-us,fr-fr}/ from int8 + tokenizer + ModelCard + package.json + README). pyproject.toml gains `[train]` extra with torch/transformers/datasets/onnx/onnxruntime/numpy/pyyaml/tqdm/pyarrow. 8/8 mailwoman_train pytest tests pass. | smoke train
2026-05-17 21:41 | phase-2 | end-to-end smoke pass — `python -m mailwoman_train smoke` ran train (200 steps × batch 16 × 2L/128H model, ~17 steps/s on CPU, loss 2.44 → 0.82 in ~12s) → eval (74 golden entries, macro-F1 ~0.01, mean conf 0.52 — expected for an undertrained tiny model; proves the eval pipeline emits real numbers in every column of the report) → ONNX export via torch.onnx.export(dynamo=True) (legacy TorchScript path crashes on transformers ≥5's masking_utils sdpa_mask; dynamo+onnxscript works) → parity check (32 val samples, max_abs_diff = 1.67e-6, well under 1e-4 tolerance) → int8 dynamic quantization (2.3 MB model.onnx) → packages/neural-weights-{en-us,fr-fr}/ assembled with model.onnx + tokenizer.model + model-card.json + package.json + README (smoke-build warning prominent). Full geometry benchmark: 8.87M params at ~0.85 train-steps/s on CPU at batch=32 → 50k steps would be ~16h on this container. Full production training stays on the operator's GPU host. | LOG + DECISIONS + push
2026-05-17 22:00 | phase-2 | operator clarification: GPU IS available (Radeon 780M / gfx1103, not NVIDIA — nvidia-smi returned nothing earlier; misread). Built ~/training-venv with ROCm 6.2 PyTorch (torch 2.5.1+rocm6.2). With HSA_OVERRIDE_GFX_VERSION=11.0.0, GPU reports 14.6 GiB VRAM, 3.07 TFLOPS bf16 1024² matmul (matches operator's ~3 TFLOPS benchmark). Retrained tokenizer-v0.1.0 on corpus-v0.1.1 (1M US + 1M FR raws, 2M lines total); sha256 82d126743d240c38beb58d9d0c2fd4b0af5f91f03aedd5db054c028f5f33c1a1. Replaced BertForTokenClassification with MailwomanCoarseEncoder — hand-rolled (nn.MultiheadAttention + LayerNorm + FFN per pre-norm block, no nn.TransformerEncoderLayer per gfx1103 incompatibility), force_math_sdpa() at every CLI entry point, model cast directly to bf16 (autocast hangs at batch≥64). Empirical batch envelope: B=64 stable, B≥96 GPU hangs at first training step (~7s into the step, before backward). 5.4 steps/s × B=64 = 345 samples/s sustained, ~2× operator's spec'd 175. Effective batch 128 via grad_accum=2; 50k steps ≈ 100k micro-batches ≈ 5-6h wall. ONNX export forced to CPU (bf16 ROCm tracing hangs); dynamo path lands via "internal Dynamo apis" fallback (onnxscript optimize then runs CPU-only). Smoke pipeline green end-to-end on GPU at B=16 bf16. | launch full Stage 1 training in background
2026-05-18 01:10 | phase-2 | Stage 1 training: stopped at step 6000 (best checkpoint). Trajectory: macro_f1(BIO-label) peaked at step 4000 = 0.372, val_loss bottomed step 6000 = 0.86; both regressed by step 8000 (val_loss=1.12, macro_f1=0.352) — clear overfitting under cosine decay still near peak LR. Earlier crash investigation found two latent bugs in the pipeline that were fixed mid-session: (1) data_loader yielded rows in parquet order causing 30× val/train divergence — fixed with 16k-row shuffle buffer + per-shard / per-rowgroup / per-row permutation; (2) MailwomanCoarseEncoder._init_weights zeroed every 1D parameter which included nn.LayerNorm.weight (gamma), collapsing each LN to constant-0 output — fixed by restoring gamma=1, beta=0 for every LN module after the blanket loop; verified by 200-step bench (broken: loss 1.85 plateau; fixed: loss 0.60 → still descending). Added full checkpoint resume (optimizer.pt + scheduler.pt + step in training_state.json, scheduler fast-forward fallback for pre-feature checkpoints) and packages/corpus-python/scripts/train_with_resume.sh for auto-restart on gfx1103 HW Exception (GPU Hang fires every 30-60 min under sustained load — wrapper kicked in 6+ times, resumed cleanly each time). Reduced save_every_steps 5000 → 2000. Final golden eval on step-006000 (74 entries, 10 US + 10 FR + 54 adversarial): per-component F1 country=0.000 region=0.104 locality=0.042 postcode=0.000 — all far below Phase 2 §6 95% target. Mean token confidence 0.865 (well-calibrated misclassification). Diagnosis: training corpus is ~75% wof-admin single-name entries ("Burlington" / "Vermont" / "Burlington, VT, United States of America"); model learns positional heuristic "first-token=locality, middle=region, end=country" but doesn't generalize to street-prefixed golden entries which assign O to street tokens. Per Phase 2 §"When to ship vs train more": <90% F1 = stop and re-examine, not train longer. Recommended v0.2.0 retrain: source-weighted sampling (boost BAN + TIGER street-level rows ~5×), OR synthesize random street prefixes onto WOF rows. ONNX export verified parity 3.2e-5 < 1e-4 over 1000 val samples; int8 dynamic quantization 37.6MB → 25.2MB (under 30MB target). neural-weights-{en-us,fr-fr}/ rebuilt with the production weights; READMEs honestly report below-target status. | commit + push + status=ready
2026-05-18 21:33 | phase-2 | v0.2.0 shipped (PR #53). source_weights mechanism + relaxed coarse-presence gate. Loader interleaves ban/tiger/nppes/state-tx/wof-admin/wof-postalcode per batch instead of mono-source blocks; wof-admin oversampling dropped to 1.0 because corpus-v0.2.0 (263.7M aligned rows, 6 train sources, full BAN + full TIGER + NPPES + IMLS + HRSA + state-*) is naturally more balanced. Trained step-050000 in 23,520s on gfx1103. Golden v0.1.0 small-slice eval: macro_f1 0.335 (9× v0.1.0), conf>0.9 calibration 0.882 (2.6× tighter). Per-component F1: region 0.83, locality 0.65, postcode 0.86, country 0.00 (support=6). Honest below-target ship on country/region/locality but real measurable improvement. neural-weights-{en-us,fr-fr}@2.0.0 published to npm. | iterate (v0.3.0 Stage 2)
2026-05-22 10:25 | phase-2 | v0.3.0 → v3.0.0 shipped (PR #115). Stage 2 label expansion (15 → 21 BIO; +venue/street/house_number) + linear-chain CRF decoder over a frozen BIO transition mask (makes orphan-I-* sequences structurally impossible — Saint Petersburg fix verified on live demo) + dual loss (CE + 0.05·CRF NLL). Trained on corpus-v0.3.0 (677M aligned rows, +usgov-nad 57.9M). Four hparam iterations converged on lr=1.5e-4 + grad_clip=1.0 + crf_loss_weight=0.05 + label_smoothing=0.0 — v0.2.0's CE-only lr=5e-4 catastrophically diverges with CRF+LS added. Early-stopped at step 1800 of 50K on val plateau (late-best window before recurring divergence — same shape as v0.1.0's step-6000 ship). Golden v0.1.2 full eval (4,535 entries): macro_f1 0.32; new label F1 — house_number 0.78 (near issue #57 floor 0.80), venue 0.39 (below 0.60 floor), street 0.27 (below 0.70 floor); coarse F1 regressed substantially vs v0.2.0 small slice — region 0.83→0.18, locality 0.65→0.27, postcode 0.86→0.76. npm-major-bumped 2.0.6 → 3.0.0 to signal the breaking label-space change. 8 commits inside the i57 container caught 3 latent slice-4 bugs the host-side smoke didn't (CRF NaN trap, JS-side STAGE1 label hardcode, stale ../../ paths in ship_v0_3_0.sh from the repo-flatten). | iterate (v0.4.0 — recover coarse F1; per-token CRF NLL norm; JS-side Viterbi)
2026-05-22 14:11 | phase-3 | @mailwoman/neural-weights-{en-us,fr-fr}@3.0.0 published to npm. Demo cut over automatically — `mailwoman.sister.software/demo` now loads the Stage 2 model and renders the new venue/street/house_number labels in the parsed-components table (confidence bars + multi-candidate picker were the v0.2.0-era demo QoL adds that surfaced the new labels with no UI changes). | docs alignment for v0.4.0
2026-05-22 19:45 | retro | post-v3.0.0 docs sweep + container fleet wind-down. Reverted root README (clobbered to tiger/'s in the unification commit). Aligned plan/README + PHASE_2_training + ARCHITECTURE with the v3.0.0 ship + the v0.4.0 next-iteration framing. Drained postmortems + KB entries from i33 + i55 + i57 (KB entries about playpen image quirks landed in playpen's docs/contributions/inbox/, not mailwoman's — they're orchestrator-image findings, not model findings). i57 postmortem surfaced concrete v0.4.0 priorities: per-token CRF NLL normalization to eliminate hand-weighted crf_loss_weight; JS-side label vocab read from model-card.json; grep audit for stale ../../ paths post-repo-flatten. | v0.4.0 corpus rebuild + retrain
2026-05-22 23:00 | docs | shipped 6 understanding/ + 11 concepts/ articles for the ESL-friendly KB track. Then: addresses-that-break-geocoders failure catalogue (10 categories), the-staged-pipeline synthesis (6-stage runtime), QUERY_SHAPE.md design intent (Stage 2.4 sub-system), STAGES.md interface contract (per-stage TS types + coordinator). PHASE_2's "Stage 1/2/3" vocabulary terminology renamed to "Tier 1/2/3" to disambiguate from runtime stages. | start Stage 1 + QueryShape implementation
2026-05-23 01:30 | runtime-pipeline | shipped Stage 1 (@mailwoman/normalize) + Stage 2.4 boundary (@mailwoman/query-shape) workspaces. Normalize: NFC + punctuation + whitespace (always) + abbreviation expansion + case-fold (opt-in), composed offsetMap traces normalized→raw. QueryShape: character class, segments, known postcode/PO-box format detection. 111 new tests (66 query-shape + 45 normalize). Pure functions, zero ML, structural typing keeps neural→core cycle-free. PHASE_7 plan doc captures both slices. | JS Viterbi for Stage 4a
2026-05-23 01:50 | neural | JS Viterbi decoder (Stage 4a) — neural/viterbi.ts + buildBioTransitionMask. Default decode mode flipped from per-token argmax to Viterbi with the BIO structural mask, making orphan-I-* sequences structurally impossible at JS runtime without retraining the model. Composes additively with learned transitions when a future weights release ships them. 15 new tests; all 1458 existing pass. v0.4.0 issue #116 item (5) is done. | Stage 6 candidate-list + QueryShape encoder prior + runPipeline coordinator
2026-05-23 02:15 | runtime-pipeline | three items: Stage 6 alternatives field on AddressNode (Springfield-class disambiguation surfaces runner-up resolutions); QueryShape soft-prior bias in NeuralAddressClassifier.parse({queryShape}) nudges Viterbi toward known-format labels (encoder still wins when confident, confidence reported is encoder-raw); runPipeline coordinator in @mailwoman/core/pipeline + createRuntimePipeline factory in mailwoman/. Structural typing throughout so core has no deps on neural/normalize/query-shape. 44 new tests; suite at 1502/1502. | kind classifier (Stage 2.5) + CLI default flip
2026-05-23 03:15 | runtime-pipeline | Stage 2.5 kind classifier @mailwoman/kind-classifier — rule-based scorers for 7 QueryKinds (postcode_only / locality_only / structured_address / intersection / po_box / landmark / vague), wired in as createRuntimePipeline's default classifyKind. CLI parse default flipped to runtime pipeline; --isolated is the new flag for the legacy rule-only path. --resolve no longer requires --neural. Graceful fallback to rule-only when neural weights aren't installed. Latent bug fix: canShortCircuit's endsWith("_zip") false-negatived us_zip4 — centralized POSTCODE_FORMATS set. 34 new tests; suite at 1536/1536. | docs sweep + web demo integration
2026-05-23 04:30 | phase-2 | v0.4.0 training-side prep landed (issue #116). Per-token CRF NLL normalization (`reduction="per_token"` on LinearChainCRF.forward) + optional class-weighted CE (`class_weights` dict on ModelConfig + tensor on encoder) + source-weight rebalance (drop usgov-nad 2.0→1.0; bump wof-admin/wof-postalcode 1.0→2.0). New `configs/v0_4_0.yaml` ties them together with lr=5e-4 back-to-baseline (v0.3.0's 1.5e-4 was only safe because crf_loss_weight=0.05 masked the dual-loss instability the per-token norm fixes). v0.3.0 backward compat preserved via field defaults ("per_sequence" + None). save/load round-trip verified through config.json. 31 pytest tests pass (28 + 3 skipped). Ready for `HSA_OVERRIDE_GFX_VERSION=11.0.0 CONFIG=src/mailwoman_train/configs/v0_4_0.yaml scripts/train_with_resume.sh`. | operator kicks off the run; eval + ship per `ship_v0_3_0.sh` template
2026-05-23 05:15 | neural | v0.4.0 issue #116 §5(a) — JS runtime label vocab now read from `model-card.json` at load time. `build_model_card` writes a `labels: list(ACTIVE_BIO_LABELS)` field; `resolveWeights` surfaces `modelCardPath`; `NeuralAddressClassifier.loadFromWeights` reads it via `readLabelsFromModelCard` and threads through to the constructor. STAGE2_BIO_LABELS remains as the legacy fallback for v3.0.0 bundles that predate the field (safe because Stage 2 strictly extends Stage 1). Backfilled the en-us + fr-fr v3.0.0 cards so dev resolves keep working. 10 hermetic JS tests (`test/model-card-labels.test.ts`) + 1 Python test (`test_model_card_carries_bio_labels_in_emission_order`); neural suite 116/116 passing. | operator-driven v0.4.0 training run remains the gating item
