# === v3.22.0-bundle-v385feed-8k — PRODUCTION BUNDLE CANDIDATE, INCREMENT 4 (Option-A Phase 3). ===
# (Claude-owned; operator go 2026-07-28 "alright let's continue", after the Decision-B analysis.)
#
# WHAT THE DECISION-B ANALYSIS SHOWED (record: .superpowers/sdd/progress.md 2026-07-28): the two
# ablated-column golden deficits are FEED-PASSENGER damage, not bundle damage — (1) the po-box
# regression is Canadian alphanumeric-postcode rows (box number stolen as postcode; 93-vs-9
# asymmetric), diluted by +22u of probe-era source mass; (2) us.street losses are ALL
# trailing-directional cuts ("74TH ST SE"→"74TH ST") — the GB boundary habit from synth-gb 10.0,
# a shard weighted for the FALSIFIED dep-loc resurrection arc. Punct-drop was tested and
# exonerated (control fails comma-intact rows too). The deploc feed was probe tooling inherited
# for comparability; production owes it nothing.
#
# ONE NAMED DELTA vs v3.21.0:
#   data: = the SHIPPED v385 recipe's data section VERBATIM (v3.8.5-latam-8k: corpus
#   v0.13.0-latam, its source_weights, its augment probs incl. punct_drop 0.3) + the two bundle
#   lexicon paths. CONSTRUCTION GOAL: the ablated column reproduces v385's golden profile because
#   it eats v385's food — Decision B dissolves. The deploc additions (gb/nz/es-pedania/fr-lieudit
#   + punct_drop 0.6) return LATER one at a time, each with its own promotion case.
#
# PRE-REGISTERED GATES — reshaped by the operator's Decision A (inputMode fragmented|formatted;
# formatted runs channels-OFF, so the SHIP columns are:)
#   G1 fragment bars (fed, P0 grid --lexicon v5 --street-lexicon v2, every save): homonym ON−OFF
#      ≥ +0.050 asis and ≥ +0.25 lower+heal; house-number classes ≥ −0.020 asis raw; lowercase-heal
#      ALL ≥ +0.015. NAMED RISK: bare-locality classes lose the es-pedania/fr-lieudit feeders —
#      if a bare-locality bar alone misses, add back JUST those two shards as one named delta.
#   G2 ablation: OFF column vs the v385 same-grader reference ONLY (the feed control is
#      deploc-specific and retires with the feed).
#   G3 invariance (int8, --baseline v385): zero new classes.
#   G4 gauntlet ×3 (fed weights-cache).
#   G5 golden ABLATED (the formatted-mode ship column): ALL v6.0.0-shipped-baseline floors — no
#      named-watch exemptions this time; v385's food means v385's floors, full stop.
#   G5b golden FED (recorded, NOT ship-gating under Decision A): the fragment-mode-on-formatted
#      read; feeds the future misroute-cost gate (misroute rate × this harm).
#   G6 canary: recorded under BOTH modes; zero-flip required only in the formatted/ablated mode
#      (Decision A routes these full-address rows there in production).
#   VERDICT: G1-G5 + G6-formatted ⇒ the promotion battery + the inputMode implementation arc.
#   Ablated-column golden failure ⇒ the feed-passenger diagnosis is FALSIFIED — bundle training
#   itself erodes base competence; STOP, architecture question (from-scratch vs shelve) to the
#   operator. No knob iteration inside the run.
#
# Launch: modal run -d corpus-python/modal/train_remote.py --config v3.22.0-bundle-v385feed-8k.yaml --resume none
#
data:
  corpus_dir: /data/corpus/versioned/v0.13.0-latam/corpus-v0.13.0-latam
  tokenizer_dir: /data/models/tokenizer/v0.9.0-multisplice
  max_length: 128
  country_weights: # VERBATIM v2.4.1.
    US: 1.0
    FR: 1.0
    DE: 1.0
    ES: 1.0
    IT: 1.0
    NL: 1.0
    PT: 1.0
    BE: 1.0
    PL: 1.0
    AT: 1.0
    CH: 1.0
    CZ: 1.0
    DK: 1.0
    "NO": 1.0
    SE: 1.0
    FI: 1.0
    IE: 1.0
    GB: 1.0
    SK: 1.0
    SI: 1.0
    HR: 1.0
    HU: 1.0
    AU: 1.0
    CA: 1.0 # NEW (Overture) — live locality, sidesteps NZ's dead dependent_locality
    MX: 1.0 # NEW (Overture) — BR dropped (0% OA-lineage + SN-garbage numbers)
  source_weights: # v3.1.0 weights + overture-latam (CA/MX/BR, dedicated source so it doesn't dilute the trained EU overture rows) + synth-no-fragment (the ONE new variable); synth-no-street-led ZEROED (its on-disk part predates --exclude-surfaces and is contaminated vs the board — see the overlay note).
    overture-latam: 6.0 # NEW — CA/MX/BR Overture shards. 6.0/~142 ≈ 4.2%, within Fable's ≤5-8% new-mass band. Distinct from  (EU) so EU exposure is untouched.
    wof-admin: 2.0
    wof-postalcode: 2.0
    ban: 3.0
    tiger: 4.0
    usgov-nppes: 2.0
    usgov-hrsa-fqhc: 2.0
    usgov-imls-pls: 2.0
    usgov-nad: 1.0
    state-ia-contractors: 2.0
    state-tx-notaries: 1.5
    state-ny-notaries: 2.0
    deepseek-kryptonite: 3.0
    synth-intersection: 1.0
    synth-german: 6.0
    synth-fr-order: 3.0
    synth-fr-admin-split: 6.0
    synth-unit: 1.5
    synth-po-box-cedex: 1.5
    synth-affix: 2.0
    synth-country: 2.0
    overture: 3.0
    gnaf: 6.0
    synth-anchor-absorption: 6.0
    synth-fr-bare-street: 12.0
    synth-si-bare-village: 12.0
    synth-no-street-led: 0.0
    synth-cz-pcfirst-preposition: 12.0
    synth-nl-postcode: 6.0
    # PROBE 1: the fragment/autocomplete shard (assay weight — moderate precedent).
    synth-fragment: 6.0
    # #727 T2 NEW — the ONE variable. 12.0 = the targeted-fix precedent (fr-bare-street,
    # si-bare-village, no-street-led are all 12.0). Streets with NO house number + the
    # bare-locality counter.
    synth-fr-fragment: 12.0
    # Track B — the NO house-number-licence lever. 12.0 = the targeted-fix tier
    # (matches fr-fragment). Bare streets + street+number with NO postcode partner, + counters.
    synth-no-fragment: 12.0
  train_rows_per_epoch: 1000000
  val_rows: 4096
  coarse_filter: true
  augment_directional_prob: 0.3
  augment_region_prob: 0.3
  augment_glue_prob: 0.25
  # #1101 FIX — the delimiter-free augmentation (built+tested, never shipped): an extra comma-stripped
  # copy per row so the model segments street/locality without leaning on the comma. Closes the gauntlet
  # comma-drop INV (White House + FR Rue du Chevaleret). ONE new variable vs v373.
  augment_punct_drop_prob: 0.3
  anchor_lookup_path: /data/anchor/pilot-anchor-lookup.json
  gazetteer_lexicon_path: /data/gazetteer/anchor-lexicon-v1.json
  # #1104 country-lexicon channel — the dedicated country soft-feed vocabulary (feature_dim 2).
  country_lexicon_path: /data/gazetteer/country-surface-lexicon-v1.json
  gazetteer_choreography: true
  affix_relabel_lexicon_path: /data/gazetteer/affix-relabel-lexicon-v1.json
  anchor_paint_mode: shaped
  anchor_value_mode: posterior_latlon

  # Option-A bundle channels (v3.22): the SAME lexicons the runtime resolves (v2/v5 — the
  # collision-curated pair). Training and inference share one painting.
  street_type_lexicon_path: /data/gazetteer/street-type-lexicon-v2.json
  locality_surface_lexicon_path: /data/gazetteer/locality-surface-lexicon-v5.json
model: # VERBATIM v2.4.1.
  hidden_size: 384
  use_span_boundary_head: true
  use_deploc_head: false # P-A: OFF — orthogonal to P-B's head experiment; don't confound the street-type read.
  span_boundary_loss_weight: 0.5
  num_hidden_layers: 6
  num_attention_heads: 6
  intermediate_size: 1536
  max_position_embeddings: 128
  type_vocab_size: 1
  hidden_dropout_prob: 0.1
  attention_probs_dropout_prob: 0.1
  use_crf: true
  crf_loss_weight: 0.0
  crf_normalization: per_sequence
  label_smoothing: 0.1
  use_locale_conditioning: true
  locale_loss_weight: 0.3
  use_postcode_anchor: true
  use_gazetteer_anchor: true
  gazetteer_feature_dim: 5
  # #1104 country channel ON — dedicated 2-dim [country_surface, country_ambiguous] soft feed with its
  # own projection + confidence (must equal the lexicon's feature_dim). Default-False elsewhere, so a
  # resume from a pre-country checkpoint stays byte-identical until this flips.
  use_country_anchor: true
  country_feature_dim: 2
  # P-A street-type channel ON — a dedicated 1-dim street_type soft feed with its own projection +
  # confidence (must equal the lexicon's feature_dim=1). Separate from the gazetteer, so init_from v385
  # loads clean (v385 lacks street_type_* → strict=False leaves ONLY those fresh).
  use_street_type_anchor: true
  street_type_feature_dim: 1
  # v3.16.0 — the locality-surface channel ON (the bundle).
  use_locality_surface_anchor: true
  locality_surface_feature_dim: 2
  # #1104 homograph-guard SOFTENER — the ONE variable vs v263. Scales the country_ambiguous dim by 0.5
  # before the country_projection (bakes into the exported ONNX; no lexicon/inference change). v263's
  # hard guard (scale 1.0) over-suppressed homograph countries Georgia/Jordan (country-homograph F1
  # 89.8->82.6); softening should recover homograph recall while holding the WOF-admin country win.
  country_ambiguous_scale: 0.5
  class_weights:
    O: 0.5
    B-country: 3.5
    I-country: 3.5
    B-region: 1.5
    I-region: 1.5
    B-locality: 2.0
    I-locality: 2.0
    B-dependent_locality: 1.0 # DELTA (0.3 -> 1.0) — the third suppressor: re-init + hot LR are useless under a 0.3 downweight.
    I-dependent_locality: 1.0 # DELTA (0.3 -> 1.0) — see B-dependent_locality above.
    B-postcode: 1.5
    I-postcode: 1.5
    B-subregion: 0.3
    I-subregion: 0.3
    B-cedex: 1.0
    I-cedex: 1.0
    B-venue: 1.5
    I-venue: 1.5
    B-street: 2.0
    I-street: 2.0
    B-house_number: 1.5
    I-house_number: 1.5
    B-street_prefix: 1.5
    I-street_prefix: 1.5
    B-street_suffix: 1.5
    I-street_suffix: 1.5
    B-unit: 1.5
    I-unit: 1.5
    B-po_box: 1.5
    I-po_box: 1.5
    B-intersection_a: 1.0
    I-intersection_a: 1.0
    B-intersection_b: 1.0
    I-intersection_b: 1.0
  use_phrase_priors: true
  phrase_feature_dim: 10

train: # Same-vocab init_from fine-tune off the SHIPPED v385 (fresh optimizer; --resume none on a fresh
  # output_dir honors init_from, weights only).
  output_dir: /data/output-v3220-bundle-v385feed-8k-s42/checkpoints # v3.22.0 production candidate.
  seed: 42
  init_from: /data/output-v384-latam-probe-s42/checkpoints/step-008000 # DELTA — the verified v385 (6.6.0) checkpoint. See header for the volume-verification evidence.
  batch_size: 128
  eval_batch_size: 128
  grad_accum_steps: 1
  learning_rate: 1.0e-5
  weight_decay: 0.01
  warmup_steps: 500
  grad_clip_norm: 1.0
  max_steps: 8000 # Production duration (the v385 precedent). Checkpoints every 2k, EACH graded (the over-trust watch).
  eval_every_steps: 2000
  save_every_steps: 2000
  log_every_steps: 50
  precision: bf16
  num_workers: 0
  csv_log_path: "{output_dir}/../train_log.csv"
  lr_schedule: constant
  gazetteer_curriculum: false
  evidence_curriculum: true # v3.16.0 — the anti-over-trust leg (ramped zero-out on BOTH bundle channels).
  evidence_noise_prob: 0.15 # v3.21 DELTA (the ONE variable): FALSE-evidence curriculum — see train.perturb_evidence_noise. 0.15/row/channel, anchor-curriculum schedule (25%→50% ramp).
  classifier_learning_rate: 1.0e-3 # P-A: hot LR for the FRESH street_type channel (the carveout covers street_type_projection/cue) + the classifier. Single phase, 3k window.
  trackio_project: mailwoman
  trackio_run_name: v3.19.0-bundle-8k-s42

eval:
  golden_dir: /data/eval/golden/v0.1.2
  val_jsonl: ""
